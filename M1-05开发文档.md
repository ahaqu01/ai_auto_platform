# M1-05 开发文档

> Owner：平台工程
> 日期：2026-08-27
> 状态：IMPLEMENTED_LOCAL
> 前置版本：3370f22（m1-04-contract-baseline-implemented）

## 目标与边界

M1-05 完成企业邀请和 Owner/Admin/Member 权限矩阵。Owner 降级、移除及最后 Owner 并发保护属于 M1-06；本阶段对 Owner 目标明确返回 403。

## 实现

- 新增 organization_invites 表和 Alembic 20260827_04。
- 邀请 token 使用 256 位随机值，数据库只保存 SHA-256 摘要；明文仅创建响应返回。
- 邀请默认 7 天过期，支持列表、撤销、重发和单次接受。
- 接受时使用行锁，校验撤销、过期、已接受状态及当前身份邮箱。
- 新增成员列表、角色调整和成员移除 API。
- Owner/Admin 可管理邀请和非 Owner 成员；Member 只读；非成员保持 404 隐藏。
- 项目创建收紧为 Owner/Admin，Member 仍可读取。
- 被移除成员立即失去组织及项目访问权。
- 成员目标路径使用 member_id，保持禁止调用方 user_id 输入的 OpenAPI 安全门。

## 验收

- 红灯：邀请模型导入失败。
- M1-05 PostgreSQL 专项：2 passed。
- API 非 PostgreSQL：166 passed，16 deselected。
- PostgreSQL 全量及迁移往返：45 passed。
- Web：5 passed，构建通过。
- npm audit：0 vulnerabilities。
- Go、文档治理和 OpenAPI 快照门禁通过。
- Demo 数据库升级至 20260827_04，API healthy，live/ready 200。

## 安全说明

邀请明文不入库、不出现在列表；邮箱大小写不敏感匹配；token 状态检查与成员写入在同一事务；租户外访问保持 404；Owner 变更不在本阶段以非并发安全方式实现。

## 后续计划

执行 M1-06 最后 Owner 并发保护，再按 M1-07 至 M1-10 推进。
