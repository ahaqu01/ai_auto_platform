# M2-03 上传会话开发文档

> Owner：平台 API / 数据库 / 安全 / QA
> 日期：2026-09-02
> 状态：ACCEPTED

## 1. 范围

M2-03 实现上传会话元数据闭环：服务端 opaque Key、请求约束、组织级配额预留、幂等创建、查询、取消和过期。它不调用对象存储，不实现分片签名、Multipart 完成或资产验证。

## 2. 数据模型

新增 `upload_sessions` 和迁移 `20260902_08`。会话初始状态为 PENDING_UPLOAD，保存组织、项目、创建者、展示名、不可变 Key、预期大小/SHA-256/类型、预留字节、过期时间和版本。数据库固定 1～20 GiB、SHA 长度/小写、状态集合、非负预留和 Key 唯一约束。

## 3. API 与授权

- `POST .../upload-sessions`：ADMIN/ENGINEER 创建，强制 Idempotency-Key。
- `GET .../upload-sessions/{id}`：项目成员读取并惰性收敛过期状态。
- `POST .../upload-sessions/{id}:cancel`：有写权限的主体幂等取消。

跨租户与非成员保持 404；VIEWER 写入返回 403；归档检查在授权后执行并返回 `409 PROJECT_ARCHIVED`。响应不包含 object_key、storage_upload_id 或任何凭据。

## 4. 配额与事务

创建前锁定组织行，串行检查活动会话数和总预留字节。默认单文件 20 GiB、企业活动会话 20、企业预留 100 GiB、有效期 24 小时。取消和过期将 reserved_bytes 清零。业务变化、幂等记录、审计和 Outbox 在同一事务提交。

## 5. 安全

Key 使用 `secrets.token_hex(16)` 生成 `v1/o/{32 hex}`，不含组织、项目、用户、日期、文件名或路径。审计和 Outbox 只记录大小、类型和状态，不记录 Key、SHA-256、Upload ID 或签名材料。

## 6. 版本管理

- 基线：`89ee4de`
- 迁移：`20260902_08`
- 目标提交：`feat: implement M2-03 upload sessions`
- CI 成功后追加验收证据，不改写已推送历史。
