# M2-07 开发文档

> Owner：平台 API / 存储 / 数据库 / 安全 / QA  
> 日期：2026-09-15  
> 状态：ACCEPTED

## 1. 目标与范围

M2-07 实现资产后台清理与对象存储对账能力：过期上传会话、遗留 Multipart、DELETING 资产、AVAILABLE 资产对象缺失、孤儿对象。它提供可由后续调度器调用的单次批处理服务，不提前实现 M2-08 前端上传与进度交互。

## 2. 核心实现

- `ArtifactMaintenanceService.run_once()` 分阶段执行，每类任务限制批量（1～1000，默认 100）。
- 过期活动会话先落库为 EXPIRED 并释放 `reserved_bytes`，再在事务外中止 Multipart；远端不存在按幂等成功处理。
- DELETING 资产在对象删除成功或对象已不存在后转为 DELETED，并记录 `deleted_at`。
- AVAILABLE 资产经 HEAD 明确返回 NOT_FOUND 后转为 FAILED；超时、临时故障、拒绝访问不产生“缺失”误判。
- 只枚举固定前缀 `v1/o/`；孤儿对象必须满足 opaque Key 格式、超过保护窗口，并在删除前二次检查上传会话与资产引用。
- 外部存储调用不持有数据库事务/行锁；失败仅保存标准 `StorageErrorCode`，30 秒起指数退避，最大 1 小时。
- 会话过期、资产删除、对象缺失、孤儿删除均写 SYSTEM 审计和事务 Outbox；不记录对象 Key、Upload ID、签名 URL 或凭据。

## 3. 数据库与存储契约

迁移头：`20260915_11`。`upload_sessions` 与 `artifacts` 增加 `cleanup_attempts`、`cleanup_last_error`、`cleanup_next_attempt_at`；`artifacts` 增加 `deleted_at`。未新增租户表，既有 RLS 边界不变。

对象存储端口新增 `list_objects("v1/o/")` 与 `StorageObjectRef`；适配器拒绝其他前缀并保持取消与错误脱敏边界。

## 4. 验收与版本

实现提交：`38a4e90`。针对性测试 14 项全部通过；GitHub Actions run `34926952005` 的 baseline、postgresql、security 三组作业全部成功。详细标准与证据见 `docs/acceptance/M2-07-清理与对账-验收标准.md` 和验收记录。

## 5. 已知边界

本阶段交付清理/对账服务和可观测结果对象，不包含生产调度部署与 M2-08 UI。真实 OSS/MinIO 网络端到端仍受既有安全网关/环境边界约束；本次未使用、写入或输出长期访问密钥，也不把模拟存储测试宣称为真实 OSS 验收。