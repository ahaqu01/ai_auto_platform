# AI 自动化工程平台开发交接文档

> 最后更新：2026-08-20  
> 项目目录：`/home/diffgram/workspace/ai_auto_platform`  
> 当前分支：`main`  
> 文档原则：本文不记录任何账号、密码、AccessKey、SSH 密码或其他密钥。

## 1. 当前任务与建设目标

本项目正在从早期的 Prexpand 简易版本，升级为面向企业和工程团队的 AI 工程化与交付平台。平台目标不是只提供单一标注或训练功能，而是形成以下完整闭环：

1. 用户、企业、团队和权限管理。
2. 项目、数据集、标注任务、数据版本管理。
3. 模型训练、评估、转换、量化和版本管理。
4. 多芯片、多设备部署与真实设备验证。
5. 工作流编排、异步任务调度、日志、指标和审计。
6. 云端控制面与本地算力、边缘设备之间的安全协作。
7. OSS/MinIO 对象存储、PostgreSQL 元数据存储和 Redis 缓存。

当前处于第一阶段基础能力建设期，重点是搭好可持续编码的工程骨架，并优先打通：

`身份与租户 → 项目 → 数据 → 任务 → 训练/部署`。

## 2. 已完成内容

### 2.1 需求与设计文档

已经阅读原始路演 PPT 第四页之后的内容，并形成以下文档：

- `平台概要.md`
- `项目详细介绍.md`
- `详细设计.md`
- `docs/design/01-产品需求规格说明书.md`
- `docs/design/02-系统总体架构与模块边界.md`
- `docs/design/03-数据库ER模型与API契约.md`
- `docs/design/04-第一阶段迭代任务与验收标准.md`
- `docs/design/05-设计审计与修订记录.md`

设计审计已重点补充多租户隔离、状态机、幂等性、审计、对象存储安全、任务调度、备份恢复和凭证管理要求。

### 2.2 仓库与工程骨架

已在服务器初始化 Git 仓库和 `main` 分支，采用单仓库组织：

```text
ai_auto_platform/
├── apps/
│   ├── api/                 # FastAPI 控制面 API
│   ├── web/                 # Vue 3 管理端
│   └── agent/               # Go 节点 Agent
├── contracts/               # OpenAPI 和任务协议
├── deploy/compose/          # 本地开发基础服务
├── docs/                    # 产品、架构和详细设计
└── README.md
```

当前提交记录：

```text
7e290ae feat: add persistent organization and project APIs
b9018a6 feat: add web shell and core domain rules
a522f9d chore: bootstrap AI platform foundation
```

### 2.3 后端 API

已完成：

- FastAPI 应用工厂和生命周期入口。
- 请求追踪中间件：接收或生成 `x-request-id`。
- RFC 7807 风格的领域错误响应。
- `/health/live` 存活检查。
- 组织、项目领域模型及基础业务规则。
- 企业必须保留至少一名 Owner 的领域规则。
- 归档项目禁止继续写入资产或创建任务的领域规则。
- SQLAlchemy 2 异步数据库层。
- PostgreSQL 异步连接池和 FastAPI Session 依赖。
- 用户、企业、企业成员、项目 ORM 模型。
- Alembic 数据库迁移框架和第一版迁移。
- 企业创建、企业列表 API。
- 项目创建、项目列表 API。
- 企业成员访问校验。
- 同一企业内项目编码唯一约束。
- 数据库冲突到稳定业务错误码的转换。

当前 OpenAPI 路径数量为 4，包括健康检查、企业和项目相关路径。

### 2.4 数据库

已创建以下核心表：

| 表 | 作用 |
|---|---|
| `users` | 平台用户基础资料 |
| `organizations` | 企业/租户 |
| `organization_members` | 用户与企业的成员关系和角色 |
| `projects` | 企业下的 AI 工程项目 |
| `alembic_version` | 数据库迁移版本 |

当前迁移版本：

```text
20260820_01 (head)
```

数据库已经在真实 PostgreSQL 容器上执行过 `alembic upgrade head`，不是只在 SQLite 测试库中验证。

### 2.5 前端

已完成：

- Vue 3、TypeScript、Vite、Pinia、Vue Router 工程初始化。
- Ant Design Vue 基础组件接入。
- 平台首页和 API 健康状态展示。
- Vitest + Vue Test Utils 测试环境。
- TypeScript 生产构建配置修复。
- Ant Design Vue 组件按需注册。
- 首页路由懒加载。

优化前主 JavaScript 包约 1.52 MB，按需加载后主包降至约 419 KB，首页异步块约 1.45 KB。

### 2.6 Go Agent

已完成：

- Go Module 和 Agent 命令入口。
- 节点 ID、控制面地址、心跳周期等配置读取。
- 配置默认值和合法性校验。
- 配置单元测试。

Agent 目前还是骨架，尚未实现注册、心跳、任务领取、日志上传和设备探测。

### 2.7 开发环境与基础服务

服务器已安装并验证：

- Docker 29.1.3
- Docker Compose 5.1.4
- Node.js 22.23.1
- npm 10.9.8
- Go 1.24.6
- Python 虚拟环境
- ripgrep、curl 等开发工具

Compose 已启动：

| 服务 | 地址 | 状态 |
|---|---|---|
| PostgreSQL | `127.0.0.1:5432` | healthy |
| Redis | `127.0.0.1:6379` | healthy |
| MinIO API | `127.0.0.1:9000` | healthy |
| MinIO Console | `127.0.0.1:9001` | healthy |

所有开发依赖端口只绑定服务器回环地址，没有暴露到局域网或公网。

Docker 已设置可访问的镜像加速源。`diffgram` 已加入 `docker` 用户组，但必须新开 SSH 登录会话后，组权限才会生效；旧会话仍需要 `sudo docker ...`。

### 2.8 当前验证结果

最后一次完整验证结果：

```text
后端 pytest：7 passed
后端 Ruff：All checks passed
前端 Vitest：1 passed
前端 TypeScript/Vite 生产构建：passed
Go Agent：go test ./... passed
PostgreSQL：healthy
Redis：healthy
MinIO：healthy
Alembic：20260820_01 (head)
API /health/live：HTTP 200
```

后端测试覆盖：

- 存活检查和请求追踪 ID。
- 最后一名企业 Owner 不允许降级或移除。
- 项目归档后的写入保护。
- 企业与项目持久化。
- 非企业成员不可访问企业项目。
- 项目编码重名返回 `409 PROJECT_CODE_EXISTS`。

## 3. 当前运行状态和启动方式

### 3.1 基础服务

```bash
cd /home/diffgram/workspace/ai_auto_platform
sudo docker compose -p ai-auto-platform \
  -f deploy/compose/docker-compose.yml up -d

sudo docker compose -p ai-auto-platform \
  -f deploy/compose/docker-compose.yml ps
```

新 SSH 会话确认 Docker 组权限生效后，可以去掉 `sudo`。

### 3.2 数据库迁移

```bash
cd /home/diffgram/workspace/ai_auto_platform/apps/api
../../.venv/bin/alembic upgrade head
../../.venv/bin/alembic current
```

### 3.3 后端开发服务

当前 API 是通过 `nohup` 临时启动在 `127.0.0.1:8000`，尚未配置 systemd 或生产进程管理：

```bash
cd /home/diffgram/workspace/ai_auto_platform
.venv/bin/uvicorn platform_api.main:app \
  --host 127.0.0.1 --port 8000 --reload
```

检查：

```bash
curl http://127.0.0.1:8000/health/live
curl http://127.0.0.1:8000/openapi.json
```

### 3.4 前端

```bash
cd /home/diffgram/workspace/ai_auto_platform/apps/web
npm install
npm run dev
```

前端开发服务器代理 `/api` 和 `/health` 到 `127.0.0.1:8000`。

### 3.5 测试与静态检查

```bash
cd /home/diffgram/workspace/ai_auto_platform

.venv/bin/pytest apps/api/tests -q
.venv/bin/ruff check apps/api/src apps/api/tests

cd apps/web
npm test
npm run build

cd ../agent
go test ./...
```

## 4. 当前卡住或尚未完成的问题

### 4.1 身份认证尚未接入

当前最大的功能缺口是没有真实登录态。企业和项目接口临时通过 `user_id` 查询参数表达调用者，仅用于验证持久化和租户隔离，不能进入正式环境。

需要尽快完成：

- Keycloak 部署与 Realm/Client 配置。
- 注册、登录、刷新令牌、退出登录。
- JWT 签名、Issuer、Audience、过期时间校验。
- 从令牌 `sub` 获取用户身份，禁止客户端自行提交调用者 `user_id`。
- 首次登录时同步或创建本地用户资料。
- 企业 Owner/Admin/Member 权限矩阵。

### 4.2 API 进程不是可靠服务

当前 Uvicorn 只是临时 `nohup` 进程：

- 服务器重启后不会自动恢复。
- 没有统一日志轮转。
- 没有进程资源限制和失败重启策略。
- 没有 Nginx/Caddy 反向代理和 TLS。

开发期可继续使用，进入多人联调前应增加 systemd 或容器化运行方案。

### 4.3 Compose 仍使用开发默认口令

Compose 中 PostgreSQL 和 MinIO 使用明确标注为本地开发用途的默认口令。因为端口只绑定 `127.0.0.1`，当前风险可控，但不可复制到测试、预发布或生产环境。

后续应改为：

- `.env.example` 只保存变量名和无敏感默认值。
- 实际值放 `.env`，并由 `.gitignore` 排除。
- 生产使用 Docker Secret、Kubernetes Secret 或云密钥管理服务。

### 4.4 MinIO 桶和对象协议尚未初始化

MinIO 服务已经健康，但尚未完成：

- 创建开发桶。
- 私有桶策略。
- 预签名上传/下载接口。
- 对象键命名规则。
- 上传完成回调和元数据一致性校验。
- 分片上传和失败清理。

### 4.5 Redis 尚未进入业务链路

Redis 已启动并启用 AOF，但目前业务代码还没有使用。计划用于：

- 短期缓存。
- 幂等键。
- 限流计数。
- 短生命周期任务状态。
- WebSocket/SSE 事件分发辅助。

Redis 不能代替 PostgreSQL 保存权威业务状态。

### 4.6 任务编排尚未实现

设计中计划使用 Temporal 或同类持久化工作流引擎，但当前 Compose 尚未部署 Temporal，训练、转换、部署、验证等任务也没有实现。

需要在进入任务中心开发前确定：

- 第一阶段是否直接引入 Temporal。
- Temporal 自托管还是云服务。
- Worker 的语言与拆分方式。
- 任务取消、超时、重试、心跳、幂等协议。

### 4.7 Git 远程仓库未配置

当前提交只存在服务器本地仓库。尚未提供 GitHub/GitLab/Gitee 等远程地址，因此没有推送、PR、分支保护或 CI。

这属于较高风险事项，应尽早配置受控私有仓库并推送备份。

### 4.8 自动化流水线和部署体系尚未建立

当前没有：

- CI 测试。
- 镜像构建。
- 依赖漏洞扫描。
- 数据库迁移检查。
- 测试/预发布部署。
- 回滚流程。

### 4.9 已知非阻断告警

后端测试存在第三方库产生的 `StarletteDeprecationWarning`，提示未来使用 `httpx2`。目前不影响测试和运行，应在 FastAPI/Starlette 依赖链稳定后统一处理，不建议为了消除告警盲目升级。

## 5. 下一步计划

### P0：身份与租户安全基线

1. 将 Keycloak 加入开发 Compose。
2. 编写可重复执行的 Realm、Client、Role 初始化配置。
3. 实现 JWT 验证依赖和 `CurrentUser`。
4. 删除企业/项目 API 中的临时 `user_id` 查询参数。
5. 增加未登录、令牌过期、Issuer 错误、跨租户访问测试。
6. 实现企业成员邀请、成员列表、角色修改、成员移除。
7. 将 Owner/Admin/Member 权限规则落到 API 层和领域层。

验收条件：调用者身份只能来自已验证令牌；跨企业访问统一返回安全的 404/403；最后一名 Owner 保护规则在真实 API 中生效。

### P0：项目管理闭环

1. 项目详情和分页列表。
2. 项目修改、归档、恢复。
3. 乐观锁或版本号，避免并发覆盖。
4. 审计事件表，记录操作者、动作、资源和请求追踪 ID。
5. API 分页、过滤、排序统一规范。

### P1：对象存储与数据集

1. 初始化 MinIO 私有桶。
2. 实现预签名上传会话。
3. 建立 `datasets`、`dataset_versions`、`data_assets`、`upload_sessions` 表。
4. 完成数据集创建、上传、版本冻结、资产列表 API。
5. 校验对象大小、摘要、Content-Type 和所属企业。
6. 实现孤儿对象清理任务。

### P1：任务中心与 Agent 通信

1. 建立任务、任务尝试、日志索引和制品表。
2. 明确 Temporal 选型并搭建开发环境。
3. Agent 注册、心跳、能力和设备信息上报。
4. 控制面按标签、资源和芯片能力调度节点。
5. 实现任务领取租约、续租、取消、超时和重试。
6. 日志写入对象存储，数据库只保存索引和摘要。

### P1：前端管理台

1. 登录页和认证回调。
2. 企业切换器。
3. 企业成员管理页。
4. 项目列表、创建、详情、归档页面。
5. API 客户端、错误处理、请求追踪 ID 展示。
6. 权限控制路由和操作按钮。

### P2：工程化与运维

1. 配置私有 Git 远程仓库。
2. 建立 CI：Ruff、pytest、Vitest、前端构建、Go test。
3. 增加 Dependabot/Renovate 和漏洞扫描。
4. API、Web、Agent 分别构建版本化镜像。
5. 增加 Prometheus 指标、结构化日志、告警。
6. 制定 PostgreSQL/对象存储备份和恢复演练。
7. 建立开发、测试、预发布、生产配置分层。

## 6. 建议的下一迭代任务顺序

建议严格按以下顺序推进，减少返工：

```text
Keycloak 与 JWT
  → CurrentUser 与 RBAC
  → 企业成员 API
  → 项目完整 CRUD 与审计
  → MinIO 上传协议
  → 数据集与版本
  → 任务模型与 Temporal
  → Agent 注册/心跳/任务领取
  → 训练与部署流水线
```

不要先做复杂训练页面再补身份、租户和状态机，否则后续几乎所有接口都需要重写权限和数据边界。

## 7. 已踩过的坑与处理经验

### 7.1 Docker Hub DNS 污染或错误解析

现象：`registry-1.docker.io` 被解析到不正确的地址，Docker 拉取 PostgreSQL 镜像超时。

处理：在 Docker daemon 配置可用镜像加速源并重启 Docker。之后 PostgreSQL、Redis、MinIO 均成功拉取。

经验：

- 先用 `getent ahostsv4` 和 `curl -4` 区分 DNS、IPv6、代理或仓库问题。
- 不要无限重试原地址。
- 镜像源配置属于服务器级变更，应记录来源并定期检查可用性。

### 7.2 Docker Compose v1 与 Docker 29 不兼容

现象：Ubuntu 仓库中的 `docker-compose 1.29.2` 在重建容器时出现：

```text
KeyError: 'ContainerConfig'
```

原因：Compose v1 已过时，无法正确处理新版 Docker 镜像元数据。

处理：安装官方 Docker Compose 5.1.4 CLI 插件；安装前后均校验版本，并用官方 SHA-256 校验二进制。之后容器成功重建，数据卷保留。

经验：统一使用：

```bash
docker compose ...
```

不要再使用：

```bash
docker-compose ...
```

### 7.3 服务器直连 Python 官方源速度极慢

现象：约 5 MB 的 `psycopg-binary` 下载速度只有几十 KB/s。

处理：切换清华 PyPI 镜像后依赖在数秒内完成安装。

经验：服务器安装依赖出现长时间无进度时，优先判断镜像源问题；不要将下载慢误判成构建失败。

### 7.4 前端生产构建的 TypeScript 类型错误

现象：单元测试通过，但 `vue-tsc -b` 出现大量 Node 类型、`Symbol.dispose` 和第三方库声明错误。

原因包括：

- 缺少 `@types/node`。
- TypeScript `lib` 未包含 Disposable 定义。
- 第三方声明库存在版本组合兼容问题。
- Vitest 的 `test` 配置使用了 Vite 的 `defineConfig` 类型。

处理：

- 安装 `@types/node`。
- 增加 `ESNext.Disposable`。
- 对第三方声明启用 `skipLibCheck`。
- 从 `vitest/config` 导入 `defineConfig`。

经验：前端必须同时执行单测和生产构建，测试通过不代表类型构建通过。

### 7.5 Ant Design Vue 全量注册导致首包过大

现象：初始生产包约 1.52 MB。

处理：组件按需注册，加上路由懒加载，主包降至约 419 KB。

经验：不要在入口直接 `.use(Antd)` 全量注册组件；后续页面应继续按路由拆包。

### 7.6 Ruff 对 FastAPI `Depends` 的 B008 误报

现象：Ruff 将 FastAPI 推荐的 `Depends(get_session)` 参数声明报告为 B008。

处理：项目级忽略 B008，同时保留其他规则；随后自动整理 import 和格式化。

经验：静态规则应结合框架语义配置，不能为通过检查而破坏 FastAPI 依赖注入写法。

### 7.7 TypeScript 增量缓存被误提交

现象：`*.tsbuildinfo` 曾进入暂存提交。

处理：从 Git 删除并在 `.gitignore` 增加 `*.tsbuildinfo`，随后修订提交。

经验：每次提交前必须检查 `git status --short` 和 `git diff --stat`。

### 7.8 新增 Docker 组权限不会影响旧会话

现象：执行 `usermod -aG docker diffgram` 后，当前 SSH 会话仍然没有 Docker Socket 权限。

处理：旧会话继续使用 `sudo`；新开 SSH 登录后组权限才生效。

### 7.9 开发服务端口不应默认暴露

最初 Compose 使用简写端口映射，默认绑定 `0.0.0.0`。后来已修改为 `127.0.0.1`，并为三项服务加入健康检查和自动重启策略。

经验：数据库、缓存和对象存储控制台默认只能监听回环地址；需要远程访问时通过 SSH 隧道或受控反向代理，不应直接开放端口。

## 8. 安全注意事项

1. 不要将任何真实凭证写进 Git、Markdown、Compose、Dockerfile、日志或命令脚本。
2. 此前通过对话渠道出现过的云账号、AccessKey 和服务器密码应视为已经暴露，必须轮换后才能用于测试或生产。
3. 本地开发默认口令不得复用到任何共享环境。
4. 对象存储必须保持私有，通过短时预签名 URL 访问。
5. 企业 ID 和用户 ID 不能作为权限证明；所有访问都必须经过服务端鉴权和成员关系校验。
6. 日志不得记录 JWT、Cookie、密码、AccessKey 或完整预签名 URL。
7. 上传文件必须进行大小、类型、摘要和所属租户校验。

## 9. 交接后的第一组具体操作

接手者建议首先执行：

```bash
cd /home/diffgram/workspace/ai_auto_platform
git status --short
git log --oneline -5

sudo docker compose -p ai-auto-platform \
  -f deploy/compose/docker-compose.yml ps

cd apps/api
../../.venv/bin/alembic current
cd ../..

.venv/bin/pytest apps/api/tests -q
.venv/bin/ruff check apps/api/src apps/api/tests
```

确认基线无误后，从“Keycloak + JWT + CurrentUser”开始下一次提交，不要在临时 `user_id` 查询参数上继续扩展更多业务接口。
