# M1-06 最后 Owner 并发保护开发文档

> Owner：平台工程
> 日期：2026-08-27
> 状态：IMPLEMENTED_LOCAL / PASSED_LOCAL / BLOCKED_EXTERNAL

## 1. 目标与范围

M1-06 为企业成员管理补齐最后 Owner 保护。范围仅包含 Owner 角色授予、降级与移除的权限及并发一致性，不引入数据库迁移。

## 2. 实现

- 以 `organizations` 行作为成员 Owner 变更的串行化锁，使用 PostgreSQL `SELECT ... FOR UPDATE`。
- 获取锁后重新加载操作者、目标成员和 Owner 数量，避免锁前检查产生 TOCTOU。
- 仅 Owner 可授予 OWNER，Admin 不能修改 Owner。
- 降级或移除 Owner 时，若当前只剩一个 Owner，返回 `409 LAST_OWNER_REQUIRED`。
- PATCH/DELETE 成员生产 API 统一调用并发安全服务；删除 M1-05 的 `OWNER_MANAGEMENT_DEFERRED` 临时阻断。
- DTO 的角色枚举开放 OWNER，使合法的 Owner 晋升可通过契约校验。

## 3. 并发语义

同一企业的 Owner 变更按企业行串行执行。两个并发事务分别尝试降级两个 Owner，或一个降级、一个移除时：首个事务提交；第二个事务取得锁后重新计数并返回 `LAST_OWNER_REQUIRED`；最终 Owner 数严格为 1。不同企业使用不同组织行，不共享该锁。

## 4. 验收标准

1. Owner 可以将 Member/Admin 晋升为 Owner。
2. Admin 不能授予 Owner，也不能降级或移除 Owner。
3. 最后一个 Owner 不得被降级或移除，API 返回 409 和稳定错误码。
4. 两 Owner 并发降级，以及降级/移除混合竞争，均只能成功一个。
5. 生产 API 使用并发安全服务，OpenAPI 契约包含 OWNER。
6. 本地 CI、完整 PostgreSQL 套件、文档检查与 Demo 健康检查通过。
7. 形成独立提交和里程碑标签，工作树干净。

## 5. 验收结果

- 本地 CI：166 passed，18 deselected；Web 5 passed；Web build、Go、文档与部署契约通过。
- PostgreSQL：47 passed，其中 M1-06 并发用例 2 passed。
- OpenAPI 快照已更新并校验一致。
- 数据库迁移仍为 `20260827_04`，本阶段无 schema 变化。
- Demo API 重建与健康验收见验收记录。
- 外部私有远端、PR/远端 CI 与签章不在当前授权范围，状态不提升为 ACCEPTED。

## 6. 版本管理

- 分支：`main`
- 基线标签：`m1-05-membership-rbac-implemented`
- M1-06 标签：`m1-06-last-owner-protected`
- 提交信息：`feat: implement M1-06 last owner concurrency protection`

## 7. 后续计划

按既定计划进入 M1-07；进入资产/任务域前必须完成 M1-08、M1-09。
