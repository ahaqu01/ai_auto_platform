# M2-08 开发文档

> Owner：Web / 平台 API / 对象存储 / 安全 / QA  
> 日期：2026-09-15  
> 状态：ACCEPTED / REAL MINIO & OSS E2E PASSED

## 1. 范围

M2-08 完成 Web 数据资产上传闭环：组织与活动项目选择、文件校验、增量 SHA-256、Multipart 分片、实时进度、暂停、恢复、取消、失败重试和服务端完整性结果。没有在浏览器持久化任何长期凭据或预签名 URL。

## 2. 实现

- 新增 `/assets` 数据资产页面，并把主导航状态更新为“可用”。归档项目不会出现在可上传项目列表。
- 摘要按 4 MiB 分块读取，由增量 SHA-256 计算；不会调用整文件 `arrayBuffer()`。
- 上传按 8 MiB 分片顺序执行。每片先获取 15 分钟预签名 URL，再以无 Cookie/无 Authorization 的 XHR PUT 直传对象存储，通过上传进度事件更新 UI。
- 每片成功后读取 ETag 并登记到平台 API；失败最多重签并重试三次。
- 暂停会 Abort 当前 PUT；恢复先读取服务端会话与已登记分片，只补传缺失分片。会话终态会拒绝恢复并提示重新创建。
- 取消同时停止浏览器请求并调用服务端取消接口。摘要失败且尚未创建会话时，“恢复/重试”会安全创建新会话。
- 所有分片登记后按编号提交清单，页面只在服务端返回 AVAILABLE 等资产结果后显示完成。

## 3. 安全边界

预签名 URL 只存在于当前函数调用参数，不进入上传控制器可序列化状态、localStorage、日志或 UI；外部 PUT 明确设置 `withCredentials=false`。前端不包含 OSS AccessKey、Secret、Bucket 写权限或内部 object key。对象存储 Bucket 必须配置允许前端来源的 PUT CORS，并向浏览器暴露 ETag。

## 4. 验收与版本

实现提交：`faf08b4`。Web 单测 17 项通过；`vue-tsc` 与 Vite 生产构建通过；全仓 `scripts/ci.sh` 通过。GitHub Actions run `34928585776` 为 `success`，最终提交证据记录在 `docs/acceptance/M2-08-Web上传闭环-验收记录.md`。

数据库迁移无变化，当前 head 仍为 `20260915_11`。

## 5. 真实存储终验

M2-CLOSE-02 已完成真实 MinIO 浏览器 E2E；M2-CLOSE-03 已完成真实阿里云 OSS 浏览器 E2E。两套链路均覆盖 1 KB、100 MB、1 GB、暂停恢复、取消、故障重试、摘要校验、资产列表、详情和授权下载。M2-CLOSE-04 进一步完成故障注入、真实 PostgreSQL/OSS 联合状态矩阵、调度与敏感日志扫描。因此 M2-08 的外部存储验收已关闭。
