# M2-04 Multipart 开发文档

> Owner：平台 API / 数据库 / 存储 / 安全 / QA  
> 日期：2026-09-11  
> 状态：IMPLEMENTED / ACCEPTANCE PENDING CI

## 1. 范围

本工作包实现 Multipart 初始化、批量分片签名、分片登记、重复登记幂等、冲突检测、断点续传查询、远端取消和并发状态保护。不实现 M2-05 的 Complete、对象 Head、大小/摘要校验及 AVAILABLE 资产创建。

## 2. API

- `POST .../upload-sessions/{id}/parts:sign`：首次调用在数据库行锁保护下初始化远端 Multipart，后续调用复用远端 Upload ID；一次签名 1～100 个不重复分片，分片号 1～10000，TTL 60～3600 秒。
- `PUT .../upload-sessions/{id}/parts/{partNumber}`：登记 ETag 和分片字节数；相同内容重复登记返回原结果，不同内容返回 `409 UPLOAD_PART_CONFLICT`。
- `GET .../upload-sessions/{id}/parts`：按分片号升序返回已登记清单，用于断点续传。
- `POST .../upload-sessions/{id}:cancel`：存在远端 Upload ID 时先幂等 Abort，成功后释放预约容量并转为 ABORTED。

API 不返回对象 Key 或远端 Upload ID。预签名 URL 只出现在签名响应中，不写审计和 Outbox。

## 3. 数据与一致性

新增 `upload_parts`，以 `(upload_session_id, part_number)` 为主键，保存 `organization_id`、ETag、大小和时间戳；数据库约束分片号与正数大小。迁移 `20260911_09` 启用并强制 FORCE RLS。

Multipart 首次初始化使用 `SELECT FOR UPDATE` 串行化同一会话，避免并发请求产生多个活动 Upload ID。初始化和首次分片登记分别写审计与 Outbox；重放不重复制造分片记录。

## 4. 存储运行时边界

路由只依赖 M2-02 的 `ObjectStoragePort`。未注入安全网络网关时失败关闭为 `503 STORAGE_UNAVAILABLE`。本工作包不解除 M2-02 的 SSRF 安全门，不把 OSS 长期密钥或预签名 URL 写入 Git、数据库、日志、审计或 Outbox。

## 5. 验收

专项矩阵覆盖初始化只执行一次、批量签名、DTO 边界、分片幂等/冲突、断点续传排序、取消远端 Multipart、终态拒绝、运行时未配置时失败关闭、迁移和 RLS。最终提交号、全量测试、PostgreSQL 和 CI 运行号记录在同名验收记录中。
