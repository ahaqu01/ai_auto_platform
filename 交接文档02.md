# AI 自动化工程平台交接文档 02

> 最后更新：2026-08-24
> 项目目录：`/home/diffgram/workspace/ai_auto_platform`
> 当前分支：`main`
> 本文基线：`27228e845ed9d0cc9e1ec702acf1c0a85b490a25`（`m0d-02-visual-console`）
> 安全约束：本文不记录 SSH、云账号、AccessKey、数据库口令或其他真实凭据。

---

## 1. 交接摘要

项目已从“只有工程骨架和后台测试”推进到“可在局域网访问的阶段性可视化管理台”。

当前能够直接查看：

- 平台管理台首页；
- 八个一级业务模块入口；
- 四类平台能力概览；
- 数据到交付的六阶段工程闭环；
- 当前建设里程碑；
- API 实时健康状态；
- Swagger API 文档。

局域网入口：

- 管理台：`http://192.168.1.129:8080/`
- API 文档：`http://192.168.1.129:8080/docs`
- 健康检查：`http://192.168.1.129:8080/health/live`

必须准确理解当前完成度：

- M0D-01 和 M0D-02 是“内部演示部署”和“可视化管理台”完成；
- 组织/项目已有后台基础 API，但管理台尚未接入登录和业务数据；
- 数据、标注、训练、模型、芯片、设备、部署等导航目前是产品结构和路线入口，不是已完成业务页面；
- REV-01 的安全整改尚未关闭，当前环境不能作为公网或生产环境发布。

---

## 2. 当前任务

### 2.1 刚完成的任务

刚完成 `M0D-02 可视化管理台`：

1. 建立验收标准和测试用例；
2. 实现前取得预期红灯 `3 failed`；
3. 实现管理台框架和工作台；
4. 前端测试、TypeScript 和 Vite 构建转绿；
5. 只滚动替换 Web 容器，没有重启数据库和 API；
6. 从开发机通过局域网验证新版本；
7. 提交 Git 并创建标签。

最终版本：

- 提交：`27228e845ed9d0cc9e1ec702acf1c0a85b490a25`
- 标签：`m0d-02-visual-console`
- 工作区：干净
- 部署状态：运行中

### 2.2 当前真正的项目阶段

当前仍处于 M0 基础设施、安全整改和可演示能力建设阶段。

功能展示虽然比此前完整，但业务闭环尚未打通。当前权威安全状态仍是：

- REV-01：`REMEDIATION_REQUIRED`
- 未关闭：2 个 P1、5 个 P2
- 权威整改顺序：`docs/plans/REV-01复审后整改计划-V4.0.md`

因此，下一名开发者不能仅根据页面观感判断平台已经进入 M1，也不能把演示环境称为生产可用。

---

## 3. 已完成内容

## 3.1 产品与设计文档

已经形成并多轮审计：

- `平台概要.md`
- `项目详细介绍.md`
- `详细设计.md`
- `docs/design/01-产品需求规格说明书.md`
- `docs/design/02-系统总体架构与模块边界.md`
- `docs/design/03-数据库ER模型与API契约.md`
- `docs/design/04-第一阶段迭代任务与验收标准.md`
- `docs/design/05-设计审计与修订记录.md`

设计内容覆盖用户、租户、项目、数据资产、标注、训练、模型、芯片、设备、交付、任务编排、对象存储、审计、监控、备份恢复等平台范围。

## 3.2 工程结构

当前仓库为单仓库：

```text
ai_auto_platform/
├── apps/
│   ├── api/                 FastAPI 控制面
│   ├── web/                 Vue 3 可视化管理台
│   ├── agent/               Go 节点 Agent 骨架
│   └── workflow-worker/     工作流 Worker 预留
├── packages/
│   ├── contracts/           OpenAPI 契约快照
│   └── schemas/             Job/Event JSON Schema
├── deploy/
│   ├── compose/             本地开发依赖
│   └── demo/                局域网演示部署
├── docs/
│   ├── acceptance/          验收标准与记录
│   ├── reviews/             严格评审报告
│   ├── plans/               整改与迭代计划
│   └── design/              产品与技术设计
└── scripts/                 契约、部署和冒烟脚本
```

## 3.3 后端与数据基础

已完成的主要后台能力：

- FastAPI 应用工厂和生命周期；
- `x-request-id` 请求追踪；
- RFC 7807 风格领域错误；
- `/health/live`、`/health/ready`；
- OIDC/JWT 验证基础；
- 用户首次认证同步；
- 组织创建和列表；
- 项目创建和列表；
- 组织成员访问校验；
- 租户隔离基础；
- SQLAlchemy 异步 Session；
- PostgreSQL ORM 和 Alembic；
- 用户、组织、成员、项目表；
- 两个迁移版本：
  - `20260820_01`
  - `20260821_02 (head)`
- 运行时 OpenAPI 导出和受控契约快照；
- PostgreSQL 隔离测试夹具和真实 API 集成测试；
- 测试数据库安全保险丝；
- 部署配置结构校验和公网地址策略。

当前后台测试最近记录：

- API 非 PostgreSQL：`123 passed, 9 deselected, 1 warning`
- PostgreSQL 专项在此前整改任务中通过；
- 仍存在 Starlette/httpx 第三方弃用警告，不影响当前运行。

## 3.4 已完成的整改包

近期已提交并打标签：

- `M0R-05R-01`：测试数据库安全保险丝
  提交 `4be82f7`，标签 `m0r-05r-01-implemented`
- `M0R-04R-01`：结构化部署地址校验
  提交 `72a3284`，标签 `m0r-04r-01-implemented`
- `M0R-04R-02`：公网端点网络策略
  提交 `6fc3888`，标签 `m0r-04r-02-implemented`
- `M0R-05R-02`：PostgreSQL 迁移断言收紧
  提交 `c0e7c2b`，标签 `m0r-05r-02-implemented`
- `M0R-05R-03`：租户 API 真实 PostgreSQL 验证
  提交 `287f232`，标签 `m0r-05r-03-implemented`
- `M0R-03R-01`：OpenAPI 身份语义与稳定导出
  提交 `f159390`，标签 `m0r-03r-01-implemented`

REV-01 复审结论：

- 提交：`6c99c85`
- 标签：`rev-01-remediation-required`
- 结论：整改未通过，必须继续 V4.0 计划。

## 3.5 M0D-01 本地演示部署

已完成：

- 独立的演示 Compose；
- PostgreSQL、Redis、MinIO、迁移、API、Web 六服务编排；
- API 和 Web 镜像；
- Nginx 同源反向代理；
- Alembic 启动迁移；
- 服务健康检查；
- 数据命名卷；
- HTTP 冒烟测试；
- 局域网唯一入口 `8080`；
- 部署和运维说明。

版本：

- 提交：`40c6ef0372d40076339b9163e9466fcc98d2e933`
- 标签：`m0d-01-local-demo-deployed`

部署文件：

- `deploy/demo/docker-compose.yml`
- `deploy/demo/api.Dockerfile`
- `deploy/demo/web.Dockerfile`
- `deploy/demo/nginx.conf`
- `deploy/demo/README.md`
- `scripts/test_demo_deployment.sh`
- `scripts/smoke_demo.sh`

## 3.6 M0D-02 可视化管理台

已完成：

- 深色品牌侧栏；
- 八个一级导航：
  - 工作台
  - 组织与项目
  - 数据资产
  - 智能标注
  - 训练中心
  - 模型仓库
  - 设备与部署
  - 系统管理
- 顶部阶段与演示环境信息；
- 四类能力概览；
- 六阶段 AI 工程交付流程；
- 实时 API 健康状态；
- 当前里程碑进度；
- 桌面和移动端响应式布局；
- M0D-02 HTML 版本标记；
- 导航和首页单元测试；
- 未完成功能的真实状态标识。

验证：

- 实现前：`3 failed`
- 实现后：`3 passed`
- Vite：3186 modules transformed
- 首页：HTTP 200
- 版本标记：`m0d-02`
- API：`ok`
- Web 容器：healthy

版本：

- 提交：`27228e845ed9d0cc9e1ec702acf1c0a85b490a25`
- 标签：`m0d-02-visual-console`

---

## 4. 当前运行状态

检查时以下服务均在运行：

- PostgreSQL：healthy
- Redis：healthy
- MinIO：healthy
- API：healthy
- Web：healthy
- migrate：已经成功执行并以 0 退出

网络边界：

- Web：`0.0.0.0:8080 -> 80`
- API：只在 Compose 内部网络
- M0D-01 PostgreSQL、Redis、MinIO：只在 Compose 内部网络
- 宿主机原有开发 PostgreSQL、Redis、MinIO：只绑定 `127.0.0.1`
- 没有配置公网路由、安全组或 DNS

常用检查：

```bash
cd /home/diffgram/workspace/ai_auto_platform

docker compose -f deploy/demo/docker-compose.yml ps
docker compose -f deploy/demo/docker-compose.yml logs --tail=200
DEMO_BASE_URL=http://127.0.0.1:8080 bash scripts/smoke_demo.sh
bash scripts/test_demo_deployment.sh
```

更新前端后：

```bash
cd /home/diffgram/workspace/ai_auto_platform/apps/web
npm test -- --run
npm run build

cd ../..
docker compose -f deploy/demo/docker-compose.yml up -d --no-deps --build web
DEMO_BASE_URL=http://127.0.0.1:8080 bash scripts/smoke_demo.sh
```

停止但保留数据：

```bash
docker compose -f deploy/demo/docker-compose.yml down
```

不要在未备份时执行：

```bash
docker compose -f deploy/demo/docker-compose.yml down -v
```

该命令会删除演示数据库、Redis 和 MinIO 命名卷。

---

## 5. 当前卡住或未解决的问题

## 5.1 REV-01 的两个 P1 未关闭

### P1-01：JWKS DNS 重绑定 / TOCTOU

现有逻辑先解析并验证 DNS，再由 `urllib` 自行建立连接。两次解析之间存在地址变化窗口，攻击者可能在校验通过后将连接切换到私网地址。

必须实现：

- 校验过的 IP 与实际 socket peer 绑定；
- 保持 TLS SNI 和 hostname verification；
- 每次重定向重新校验和固定地址；
- 用测试证明“验证公网、连接私网”会被拒绝。

任务编号：`M0R-04R-03`。

### P1-02：downgrade 测试目标仍不够可抛弃

当前测试库后缀、确认变量和非超级用户并不能证明整个数据库可安全销毁。未来若出现全局或 schema-qualified DDL，可能逃逸随机 schema。

必须实现：

- 每次 downgrade 新建独立一次性数据库或容器；
- owner、连接和 disposal marker 校验；
- 拒绝未经批准的 global/schema-qualified DDL；
- 销毁前二次确认。

任务编号：`M0R-05R-04`。

在该问题关闭前，不得在共享数据库或普通开发数据库上运行 downgrade。

## 5.2 五个 P2 未关闭

1. 生产 PostgreSQL/Redis TLS 未强制；
2. OpenAPI 导出会修改进程环境和 Settings cache，异常路径也未完全恢复；
3. PostgreSQL base inventory 未覆盖 view、materialized view、routine、policy、domain、collation、grant；
4. Keycloak issuer 校验不接受合法上下文前缀；
5. OSS 目前只有配置策略，没有真实客户端出站 peer 约束，不能声称运行时 SSRF 已关闭。

对应任务：

- `M0R-04R-04`
- `M0R-03R-02`
- `M0R-05R-05`
- `M0R-04R-05`

## 5.3 登录和真实管理功能未接入

虽然后台已有 OIDC/JWT 基础，但当前演示环境没有：

- Keycloak 服务和 Realm/Client 初始化；
- 登录、刷新、退出；
- 管理台用户会话；
- 组织切换器；
- 组织/项目页面与真实 API 接入；
- RBAC 页面级和按钮级控制。

因此：

- “新建项目”按钮当前故意禁用；
- 除工作台外的一级菜单当前没有独立路由；
- 不应通过临时 header、硬编码用户或关闭鉴权来制造可操作假象。

## 5.4 业务闭环尚未实现

尚未实现：

- 数据集、版本和资产上传；
- MinIO 私有桶与预签名上传；
- 标注项目、任务、分发和质量验收；
- 训练实验、任务、指标和制品；
- 模型版本、转换、量化和兼容矩阵；
- Agent 注册、心跳、资源和设备探测；
- 任务领取、租约、重试、取消和日志上传；
- 真实设备部署、验证和交付报告；
- Temporal 等持久化任务编排。

## 5.5 Git 远程与 CI 缺失

当前代码主要保存在服务器本地 Git 仓库，尚未建立：

- 受控私有远程仓库；
- 远端备份；
- PR 和分支保护；
- CODEOWNERS；
- 自动化 CI；
- 依赖漏洞扫描；
- Staging 环境。

这是持续开发中的较高运维风险。

## 5.6 状态文档滞后

`docs/CURRENT_STATUS.md` 仍停留在早期 `64bb1ea` 附近，已不能代表当前状态。

接手时状态优先级：

1. 本文；
2. 最新 Git HEAD 和标签；
3. `docs/reviews/REV-01-整改双专项复审报告.md`；
4. `docs/plans/REV-01复审后整改计划-V4.0.md`；
5. 各任务最新验收记录；
6. 最后才参考旧 `docs/CURRENT_STATUS.md` 和 `交接文档.md`。

后续应在单独的文档治理任务中更新或废弃 `docs/CURRENT_STATUS.md`，不要在安全 P1 修复提交中混入大范围文档整理。

## 5.7 自动浏览器验证受桌面环境阻塞

Codex 桌面浏览器运行时因 Windows 沙箱 ACL 初始化错误退出，无法自动生成 DOM 检查或截图。

已经使用以下替代证据：

- 开发机 `Invoke-WebRequest` 经局域网访问；
- 首页 HTTP 200；
- M0D-02 版本标记；
- Vite 懒加载资源内容检查；
- Vue 单元测试；
- 服务器 HTTP 冒烟测试。

这是本地工具问题，不是平台运行问题。如果后续要做浏览器端到端测试，建议在仓库中正式引入 Playwright CI，而不是依赖桌面会话。

---

## 6. 下一步计划

## 6.1 推荐执行顺序

除非用户明确调整优先级，建议按以下顺序：

### 第一步：M0R-04R-03

目标：关闭 JWKS DNS 重绑定 P1。

要求：

1. 先写验收标准；
2. 建立“验证 IP 与实际连接 peer 不一致”的红灯；
3. 实现地址固定；
4. 验证 TLS hostname/SNI；
5. 验证每次重定向；
6. 运行认证、配置和全量 API 回归；
7. 独立提交并打 `m0r-04r-03-implemented`。

### 第二步：M0R-05R-04

目标：关闭 downgrade 数据库隔离 P1。

要求：

1. 一次性数据库生命周期；
2. disposal marker；
3. owner/连接校验；
4. global DDL 防逃逸；
5. 只有红灯和保险丝通过后才能执行 downgrade；
6. 独立提交并打 `m0r-05r-04-implemented`。

### 第三步：清理 P2

按 V4.0 顺序：

1. `M0R-04R-04`：生产 TLS 与 Keycloak 前缀；
2. `M0R-03R-02`：无副作用 OpenAPI 导出；
3. `M0R-05R-05`：完整 PostgreSQL base inventory；
4. `M0R-04R-05`：OSS 出站策略闭环或范围降级。

### 第四步：REV-01R

顺序必须是：

1. Bugbot；
2. Security Review；
3. 非作者人工复核。

门禁：

- P1 = 0；
- P2 = 0；
- 每个发现均有测试、关闭提交和验收记录；
- 工作区干净。

### 第五步：恢复业务开发

REV-01R 通过后建议：

1. Keycloak 本地演示部署；
2. 登录/退出和管理台会话；
3. 组织与项目独立页面；
4. 组织切换与 RBAC；
5. 数据资产与 MinIO 上传；
6. 标注任务；
7. 训练任务；
8. 模型仓库；
9. Agent 与设备；
10. 部署验证闭环。

## 6.2 可选的下一可视化任务

如果用户优先要求继续看网页效果，可以建立 `M0D-03 管理台路由与组织项目页面`，但必须遵守：

- 不关闭或绕过认证；
- 可以先做路由、空状态、加载态、错误态和 API 客户端；
- 真实组织/项目数据接入应以登录完成为前置；
- 不把纯静态数据标记成真实统计；
- 不应挤占两个 P1 安全任务。

---

## 7. 已踩过的坑和处理经验

## 7.1 服务器访问 PyPI 极慢

现象：

- Docker API 镜像构建停在 5.2 MB 的 `psycopg-binary` wheel；
- 进程存活但数分钟几乎没有进展。

处理：

1. 在有 VPN 的开发机使用阿里云 PyPI 镜像下载 Linux/Python 3.12 wheels；
2. 打包并通过 SFTP 上传服务器；
3. 解压到：
   `/home/diffgram/workspace/ai_auto_platform/.demo-cache/wheels`
4. API Dockerfile 使用：
   `--no-index --find-links=/wheels`
5. `.demo-cache/` 已加入 `.gitignore`。

当前缓存：

- 约 19 MB；
- 36 个 wheel；
- 不进入 Git。

注意：

- 删除 `.demo-cache/wheels` 后，API 离线镜像重建会失败；
- 已构建镜像不受影响；
- 更新 Python 依赖后必须同步刷新 wheel 缓存。

## 7.2 SSH 辅助脚本有 60 秒读取超时

现象：

- 长时间 `docker compose up -d --build` 会使本地 Paramiko 读取超时；
- 远端前台命令可能随连接关闭而中断；
- 脚本只在远端命令结束后一次性返回 stdout，不提供持续日志。

处理：

- 短命令直接执行；
- 长构建使用后台任务写入 `/tmp` 日志，再分次检查；
- 或拆成镜像构建、启动、健康检查三个步骤；
- 不要把“本地 SSH 超时”误判为 Docker 构建失败。

## 7.3 全量 API 测试会触发 PostgreSQL 安全保险丝

现象：

直接执行：

```bash
.venv/bin/pytest apps/api/tests -q
```

在未设置测试数据库确认变量时得到：

- `123 passed`
- `9 setup errors`

根因：

- PostgreSQL 专项测试主动要求专用测试数据库和确认变量；
- 这是 M0R-05R-01 的安全设计，不是代码回归。

正确做法：

非 PostgreSQL回归：

```bash
.venv/bin/pytest apps/api/tests -q -m 'not postgresql'
```

PostgreSQL 专项必须按照验收文档配置明确的一次性测试目标，绝不能为了消除错误删除保险丝。

## 7.4 原始 HTML 不包含 Vue 页面正文

现象：

HTTP 冒烟最初检查 `AI Engineering`，但 Vue 文本由 JavaScript 渲染，原始 `index.html` 不包含该正文，因此错误失败。

处理：

- HTTP 冒烟检查 HTML title 和稳定版本 meta；
- Vue 单元测试检查渲染后的中文内容；
- 构建资源检查“项目工作台”等关键文字；
- 不用原始 HTML 替代 DOM 渲染测试。

## 7.5 浏览器可能继续显示旧版本

现象：

Web 容器已更新，但浏览器仍可能缓存旧的 index 或 JS。

处理：

- `Ctrl + F5` 强制刷新；
- 查看 HTML 中：
  `<meta name="application-version" content="m0d-02" />`
- 后续生产化应为 index 增加 no-cache，哈希资产使用长期缓存。

## 7.6 Compose 经典构建器反复发送大上下文

现象：

- 服务器未安装 buildx；
- 每次 Web 小改动也会发送约 105 MB 构建上下文；
- Compose 输出：
  `Docker Compose requires buildx plugin to be installed`。

影响：

- 不阻断构建；
- 明显拖慢前端发布。

后续优化：

1. 增加仓库根 `.dockerignore`；
2. 排除 `.git`、`.venv`、`node_modules`、测试缓存、文档和不相关应用；
3. 安装并验证 buildx；
4. Web 使用更小构建上下文或版本化制品。

该优化应单独建任务并测试，避免误排除 `apps/web/dist` 或 `.demo-cache/wheels`。

## 7.7 Web 镜像使用已测试 dist

由于服务器 npm/Node 镜像下载成本高，当前流程是：

1. 在服务器宿主机执行 `npm test`；
2. 执行 `npm run build`；
3. Web Dockerfile 将 `apps/web/dist` 复制进 Nginx 镜像。

优点：

- 镜像构建快；
- 部署的是刚刚通过测试的产物。

注意：

- `dist/` 被 Git 忽略；
- 新克隆后必须先执行 `npm run build`；
- 不能在未重建 dist 的情况下直接重建 Web 镜像。

## 7.8 patch 工具可能生成 .orig/.rej

在模糊匹配或补丁失败时，远端 `patch` 可能产生：

- `*.orig`
- `*.rej`

提交前必须检查：

```bash
find . -type f \( -name '*.orig' -o -name '*.rej' \) -print
git status --short
```

只删除本次工具明确生成的文件，不要误删用户文件。

## 7.9 M0D-01 冒烟脚本名称未随任务升级

脚本仍名为 `scripts/smoke_demo.sh`，成功输出文本仍是：

```text
PASS: M0D-01 HTTP smoke test ...
```

但内部已经增加 M0D-02 版本标记断言。它目前功能正确，只是输出名称滞后。后续可以在独立的小任务中改成通用 `demo deployment smoke test`，不影响本次验收。

## 7.10 页面丰富度与后台完成度不是同一件事

此前只有首页健康卡，虽然后台已完成大量工程和安全工作，但用户看到的内容很少，造成“平台是不是只做了这一页”的预期落差。

经验：

- 每个里程碑应同时说明“后台完成内容”和“用户可见内容”；
- 演示部署前明确当前能点什么、不能点什么；
- 页面中的规划模块必须显式标注；
- 不用漂亮页面掩盖业务未实现，也不要让大量后台成果完全不可见。

---

## 8. 版本管理规则

继续遵守：

1. 每个任务从干净工作区开始；
2. 先记录基线 commit 和 tag；
3. 验收标准和测试先行；
4. 实现前保留预期红灯；
5. 一个任务一个独立提交；
6. 提交前执行：
   - 相关测试；
   - `git diff --check`；
   - staged 文件清单；
   - 敏感信息扫描；
7. 只有达到任务定义的验收语义才打标签；
8. 安全整改包的标签只能表示 `implemented`，REV-01R 通过前不能使用 `reviewed` 或生产就绪语义；
9. 不使用 `git reset --hard`；
10. 不覆盖或清理不属于当前任务的用户改动。

常用检查：

```bash
git status --short --branch
git log --oneline --decorate -20
git tag --points-at HEAD
git diff --check
git diff --cached --name-status
git diff --cached --stat
```

---

## 9. 接手后的第一组命令

```bash
cd /home/diffgram/workspace/ai_auto_platform

git status --short --branch
git rev-parse HEAD
git tag --points-at HEAD

docker compose -f deploy/demo/docker-compose.yml ps
DEMO_BASE_URL=http://127.0.0.1:8080 bash scripts/smoke_demo.sh

cd apps/web
npm test -- --run
npm run build
```

预期：

- Git 工作区干净；
- HEAD 为本文记录的提交或其后续明确提交；
- 服务 healthy；
- HTTP 冒烟通过；
- Web 3 个测试通过；
- Vite 构建通过。

如果任一预期不成立，先查明原因，不要直接重建数据卷、删除缓存或绕过测试。

---

## 10. 关键文档索引

安全与评审：

- `docs/reviews/REV-01-整改双专项复审报告.md`
- `docs/plans/REV-01复审后整改计划-V4.0.md`
- `docs/acceptance/REV-01-整改双专项复审验收记录.md`

演示部署：

- `docs/acceptance/M0D-01-本地阶段性演示部署验收标准.md`
- `docs/acceptance/M0D-01-本地阶段性演示部署验收记录.md`
- `deploy/demo/README.md`

可视化管理台：

- `docs/acceptance/M0D-02-可视化管理台验收标准.md`
- `docs/acceptance/M0D-02-可视化管理台验收记录.md`

历史交接：

- `交接文档.md`：仅作为早期历史记录；
- `交接文档02.md`：当前交接入口；
- `docs/CURRENT_STATUS.md`：已滞后，不能单独作为当前状态依据。

---

## 11. 最终交接结论

当前成果已经达到“局域网可部署、可展示、基础 API 可验证、工程测试可持续运行”的阶段。

当前没有因代码错误导致的部署阻断，真正的阻断项是：

1. REV-01 两个 P1；
2. 五个 P2；
3. 登录与权限未形成完整用户体验；
4. 业务闭环尚未实现；
5. 远程 Git、CI 和 Staging 缺失。

下一步最优先任务不是继续堆静态页面，而是执行 `M0R-04R-03` 和 `M0R-05R-04` 清除 P1，然后完成 P2 和 REV-01R。安全门禁通过后，再将当前可视化管理台逐页连接到真实业务能力。
