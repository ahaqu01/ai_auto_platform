# M2复评整改任务开发文档

更新日期：2026-09-17

## 任务基线

- 历史验收基线：`m2-accepted`，提交 `41c22dd`，保持不可变。
- 详细评审报告：`c107912`。
- 整改分支：`codex/m2-review-remediation`。
- 原则：每道门禁独立实现、验证和提交，不以代码完成替代实际验收；不伪造人员签署。

## 顺序与验收标准

### M2-REVIEW-01：P1 运维公平性

- 对应 P1-BUG-01、P1-BUG-02。
- 资产 HEAD 成功、失败均记录持久化扫描进度，后续资产不被首批永久阻塞。
- 孤儿扫描持久化 key 游标，多批次推进、完成后回绕，进程重启不丢失进度。
- 超过两倍 batch size 的数据在有限轮次内全部被检查；后部缺失对象和孤儿被发现。
- 不改变资产产品版本号，不误删已知对象、年轻对象或非平台 key。
- Ruff、专项单元测试、PostgreSQL 迁移/RLS、安全 CI 均通过。

### M2-REVIEW-02：数据库身份拆分

- 对应 P1-SEC-01。
- migration owner、API login、maintenance login 分离，运行进程无 superuser/BYPASSRLS/CREATEROLE/CREATEDB。
- API 无法 SET ROLE 到 owner/maintenance，不能操作 maintenance state，跨租户无上下文查询被拒绝。
- maintenance 仅有上传会话、资产、维护状态和 SYSTEM 事件的必要权限，不可访问用户身份或成员权限表。
- 在隔离 PostgreSQL 验证后才切换 Staging；切换前备份角色/权限和受控配置，保持 Demo 不受影响。

### M2-REVIEW-03：P2 合同和产品

- 统一 API、OpenAPI、Gateway 的签名有效期上限和错误映射。
- Web 完成资产游标分页，后 100 条资产可查看详情和下载。
- completion 的幂等 key/payload 冲突和重放行为明确、可验证。
- 完成 OSS STS/RAM Role 迁移或输出经项目负责人明确批准的限期风险处置；不擅自删除其他服务使用的旧 Key。
- 同步修复上传范围锁定，并明确完整性校验不等于恶意文件扫描。

### M2-REVIEW-04：全量回归

- 全量 API/Web/PostgreSQL、安全扫描和 OpenAPI 漂移门禁通过。
- 真实 MinIO 批次公平性、列表分页、签名 TTL、身份权限回归通过。
- 受控 OSS Staging 验证通过，测试对象与临时资源清理并确认不泄露凭据。
- 更新总体验收证据、当前状态和评审整改索引。

### M2-REVIEW-05：四方复签

- 产品、技术、安全、QA 分别给出证据驱动的复签意见。
- AI 专家意见与人员/项目负责人签署区分；无授权不得代签真实人员。
- P1 为零，P2 关闭或有明确负责人、期限和批准的风险处置。
- 在最终候选提交上复签，再合并 main、确认 main CI，最后创建新的 `m2-review-remediation-accepted` 标签，不移动旧标签。

## 已取得证据

- `5b99fa6`：首次运维公平性实现；迁移 `20260916_12`。
- Ruff 通过；维护专项 `9 passed`。
- GitHub Actions run `35112435751` 对 `5b99fa6` 为 completed/success。
- `c728a20`：HEAD 失败批次不阻塞后续扫描；重启后孤儿扫描游标保留。
- 维护与调度专项 `18 passed`，Ruff 通过；API 全量非 PostgreSQL 回归 `281 passed, 32 deselected`。
- GitHub Actions run `35170834811` 对 `c728a20` 为 completed/success，包含 baseline、PostgreSQL 和 security 门禁。
- M2-REVIEW-01 为 `CODE/CI ACCEPTED`；真实 MinIO/OSS 规模化复验保留到 M2-REVIEW-04。迁移和维护代码已随身份拆分部署 Staging，Demo 未变。
- 已只读确认 Staging 的 `platform` 登录角色为 superuser/BYPASSRLS/CREATEROLE/CREATEDB，`platform_runtime` 无这些权限。

## M2-REVIEW-02 启动约束

身份拆分已完成，实施同时拆分角色与容器配置：仅改变 DATABASE_URL 不够，因为共享 env_file 还会让 API 继承 POSTGRES_PASSWORD 等管理员凭据。采用独立的数据库管理、API、维护服务配置文件；管理员配置不挂载到运行进程。隔离 PostgreSQL 已验证迁移、身份同步、跨租户拒绝、提交后上下文恢复和维护事件权限，再完成有备份材料的 Staging 切换。Demo 保持原状。

`d72da41` 实现、`4c81947` 运行态验证，CI `35172007557`、`35172236228` 均 success；真实 OSS Staging 浏览器 `1 passed (4.0m)`，平台前缀对象 0，临时用户/组织清理，维护 failures=0。M2-REVIEW-02 为 `ACCEPTED / STAGING SWITCH ACCEPTED`，验收标准与证据见同名开发文档和验收记录。

## 当前状态

`M2 REVIEW REMEDIATION IN PROGRESS`。第一道门禁代码与 CI 通过；第二道数据库身份拆分与 Staging 切换已 ACCEPTED。第三道 P2 功能修复已专项验收，长期 Key 的 STS 安全项仍 OPEN；全量复验及四方复签尚未完成，不提前标记通过。

## M2-REVIEW-03 当前证据

`fa74c93` 实现签名 60～900 秒、completion 幂等记录及中断恢复、资产游标分页、范围锁定和中文扫描提示；`52a552d` 新增 101 个真实 OSS 资产专项。CI `35179387752`、`35179580912` success；API 295 passed/32 deselected，Web 27 passed/build 成功；Staging Chromium `1 passed (1.3m)`，第 101 行详情/下载、TTL 边界、同 key 重放/冲突通过。临时用户/组织/对象清理，资产和会话数 0、平台前缀对象 0；受限数据库身份和健康复核通过。

P2-BUG-03/04/05 CLOSED。P2-SEC-02 未完成真实 STS/RAM Role 切换，也未获得限期风险批准，保持 OPEN；最小权限策略模板和缺少条件见《M2-REVIEW-03-OSS-STS迁移与风险处置清单》。本轮不是 M2 全量复验或四方复签，不自动合并 main 或建立新验收标签。

收尾补丁 `8979ecb` 修复 FAILED 终态会话无法结束的问题，Web 28 passed/build、CI `35180232766` success；真实 OSS 终态恢复与新上传 `1 passed (6.2s)`，资源再次清理。P2 整体仍因 STS 安全项 OPEN 而未关闭。
