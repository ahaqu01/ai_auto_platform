# M1-03 邮箱非唯一与原子身份同步验收记录

> Owner：QA / 平台工程
> 日期：2026-08-27
> 状态：PASSED_LOCAL

## 结论

真实 PostgreSQL 集成、迁移往返、全量质量门和 Demo 升级全部通过。M1-03 判定为 IMPLEMENTED_LOCAL / PASSED_LOCAL。当前没有远端推送、PR 与外部 Staging 签章权限，因此不提升为 ACCEPTED。

## 证据

1. 红灯基线：3 failed，分别暴露邮箱唯一约束、同邮箱异主体 409、并发首次登录冲突。
2. PostgreSQL 专项：4 passed，包含同一身份 20 路并发。
3. PostgreSQL 全量：43 passed。
4. 可销毁临时数据库迁移往返：3 passed，受 owner/marker 安全门保护。
5. CI baseline：PASS；API 158 passed、Web 5 passed、Web build、npm audit、Go 与文档检查均通过。
6. Demo：Alembic 20260827_03；API healthy；live/ready 200；users 仅保留 uq_users_external_identity。

## 验收项对照

- 删除邮箱唯一约束：通过。
- 保留 issuer/subject 唯一身份键：通过。
- 20 路并发首次同步收敛为一行：通过。
- 同邮箱不同主体独立建户：通过。
- 空资料不覆盖已有值：通过。
- upgrade/downgrade/upgrade：通过。
- 全量门禁与 Demo：通过。
- 开发文档、验收标准、记录、提交和标签：通过。

## 外部阻塞

远端推送、PR 检查和外部 Staging 签章未执行。取得外部证据前不得标记 ACCEPTED。
