# M0D-02 可视化管理台验收记录

## 1. 结论

**通过。** 可视化管理台已部署到 `http://192.168.1.129:8080`。页面呈现当前真实能力与建设路线，不将规划模块伪装为已完成。

## 2. 版本基线

- 基线提交：`40c6ef0372d40076339b9163e9466fcc98d2e933`
- 基线标签：`m0d-01-local-demo-deployed`
- 计划标签：`m0d-02-visual-console`

## 3. 测试先行

实现前测试为 `3 failed`，分别识别：

1. 平台主导航缺失；
2. 项目工作台和工程流程缺失；
3. 健康状态缺少可读的“运行正常”。

实现后同一组测试为 `3 passed`。

## 4. 验收结果

### 管理台框架：通过

- 已实现品牌侧栏、八个一级业务入口、顶部阶段状态和内容区。
- 工作台菜单具有选中态。
- CSS 提供 1120px 与 760px 两级响应式布局。
- 移动端导航转换为横向滚动，不强制固定侧栏。

### 工作台内容：通过

- 已实现项目工作台欢迎区。
- 已实现组织项目、数据资产、训练任务、设备节点四类概览。
- 已实现数据准备、智能标注、模型训练、芯片适配、设备验证、交付部署六阶段流程。
- 已实现系统健康和四阶段里程碑进度。
- 未实现模块统一标记为“规划中”；新建项目按钮在认证接入前禁用。

### 状态真实性：通过

- 继续实时请求 `/health/live`。
- 成功时显示服务名 `ai-auto-platform`、版本 `0.1.0` 和“运行正常”。
- 失败路径保留明确告警。
- 未绕过认证访问租户 API，未填充虚假业务统计数字。

### 自动化与部署：通过

- Web 测试：2 个文件、3 个测试全部通过。
- TypeScript 与 Vite 生产构建：通过，3186 modules transformed。
- HTTP 冒烟：通过。
- 开发机局域网访问：首页 200、M0D-02 版本标记存在、API 状态 `ok`。
- 新工作台资源包含“项目工作台”“AI 工程交付闭环”“规划中”。
- Web 容器已滚动替换，其他数据与 API 服务未重启。

自动浏览器 DOM/截图验证受桌面沙箱 ACL 初始化错误阻止；已使用开发机局域网 HTTP、新资源内容检查和 Vue 单元测试交叉验证，不影响部署结论。

## 5. 已知边界

- 当前一级导航除工作台外仍为路线入口，尚未建立独立路由页面。
- 组织与项目 API 已有基础能力，但登录未接入，因此管理台没有绕过鉴权调用。
- 数据、标注、训练、模型、芯片与设备业务仍需后续迭代。
- 环境仍是可信局域网内部演示，不具备公网或生产发布条件。
