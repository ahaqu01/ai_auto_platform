# M2-CLOSE-05 产品范围复核记录

> 日期：2026-09-16  
> 结论：PASS

## 范围核对

- 资产列表：按项目加载，展示文件名、大小、完整性和资产状态，并提供刷新入口。
- 资产详情：展示状态、完整性、安全扫描状态和已验证 SHA-256。
- 授权下载：仅 AVAILABLE 状态启用下载按钮；前端先向平台 API 请求 600 秒授权 URL，再执行下载。
- 状态保护：QUARANTINED、DELETING、DELETED 不提供有效授权下载；非成员、无认证和跨租户请求失败关闭。
- 归档语义：归档项目允许读取已有资产，禁止删除和新建上传。

## 证据

- `AssetsView.vue` 和 `platform.ts` 已实现列表、详情与 `:download-url` 操作。
- Web 单测现为 19 项，通过；新增列表/详情/状态下载门禁断言。
- 真实 MinIO 与 OSS Chromium E2E 均验证资产列表、详情、建议文件名、授权下载和下载内容摘要。
- M2-CLOSE-04 的真实 PostgreSQL+OSS 联合测试验证 AVAILABLE 下载、非成员、无认证、归档、QUARANTINED/DELETING/DELETED。
- 管理台曾残留“M2 96%/待实存 E2E”的过期展示；提交 `6220cf4` 已改为“M2 100%/已验收”，并由测试约束。

产品范围与 M2 计划一致，不需要额外范围豁免。
