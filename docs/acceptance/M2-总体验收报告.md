# M2 总体验收报告

> 日期：2026-09-16  
> 范围：M2-01～08、M2-CLOSE-01～05  
> 当前结论：M2 ACCEPTED / FINAL TAG GATE AUTHORIZED

## 1. 交付结论

M2 已建立资产元数据、受控上传、Multipart、完整性强校验、资产列表/详情、授权下载、失败恢复、清理对账、真实对象存储和 Web 操作闭环。M2 不包含 Agent 执行、训练编排、任意用户代码运行或后续设备交付能力。

## 2. 分项证据

- M2-01：对象 Key、双状态机、完整性与威胁模型，提交 `e69b249`，CI `33580267593`。
- M2-02：存储端口与 MinIO/OSS 契约，提交 `d4e0123`，CI `33581470484`。
- M2-03：上传会话、配额、幂等、RLS，提交 `cca6a9b`，CI `33582996780`。
- M2-04：Multipart 与分片登记，提交 `eb78d1a`、`b4b89d0`，CI `34555956515`。
- M2-05：完成与强校验，提交 `1155aae`，CI `34557468178`。
- M2-06：资产访问与授权，提交 `5cf93b6`、`4715aa0`，CI `34559105007`。
- M2-07：清理与对账，提交 `38a4e90`，CI `34927350605`。
- M2-08：Web 上传闭环，提交 `faf08b4`，CI `34928585776`。
- CLOSE-01：安全 Gateway，提交 `ea748b5`、`efe20f3`，CI `34964313687`。
- CLOSE-02：真实 MinIO Chromium E2E，提交 `34ead76`、`b3d03ba`、`d466b49`，CI `34970847263`。
- CLOSE-03：真实 OSS E2E 与新 Key 切换，最终关闭提交 `47a80c6`，CI `35081815099`。
- CLOSE-04：故障、安全、运维闭环，提交 `3024674`、`de4992d`、`99948f8`，CI `35085772619`、`35101720073`、`35102410249`。
- CLOSE-05：产品状态修正提交 `6220cf4`，文档治理和四方签署由本报告提交承载。

## 3. 真实环境与质量

- MinIO 与阿里云 OSS 均完成真实浏览器 1 KB/100 MB/1 GB、暂停恢复、取消、失败重试、完整性、详情及下载。
- API/隔离 PostgreSQL 全量回归 `309 passed, 1 skipped`；显式真实 OSS 联合用例单独执行 `1 passed`。
- complete 并发只产生一个 Artifact；远端响应丢失和 DB 提交失败均可恢复。
- 维护调度具备互斥、超时、指标、告警、心跳和退避；Staging API/维护服务 healthy。
- 敏感日志扫描覆盖 API、Web/Nginx、Keycloak、维护任务、审计和 Outbox，未检出 AK、Secret、完整签名 URL、Authorization/Cookie 或内部 upload ID。
- 测试完成后 Bucket `v1/o/` 对象数为 0，临时测试数据库和角色已删除。

## 4. 风险与范围

项目运行服务全部使用新 OSS Key。其他服务仍使用的旧 Key 是经项目负责人批准的项目范围外遗留风险，不阻塞本项目验收，但其安全属性不变，仍建议后续迁移和废止。产品范围复核确认资产列表、详情和授权下载符合 M2 计划，无新增范围豁免。

## 5. 四方签署

产品、技术、安全、QA 均为 PASS，详见 `M2-四方签署记录.md`。签署授权进入 main 发布门禁，不等于 main 已验证。

## 6. 发布证据与最终标签门禁

1. `codex/m2-assets-storage` 已通过非快进合并提交 `270eee8` 合入 main。
2. 合并提交的 GitHub Actions run `35107761452` 为 success。
3. 当前提交写回 main CI、四方签署和最终发布证据；工作树必须保持干净。
4. 当前最终证据提交的 main CI success 后，创建并推送 annotated tag `m2-accepted`，标签必须指向该最终证据提交。

功能、产品、安全、QA 与首次 main CI 门禁均已通过，M2 验收结论为 ACCEPTED。标签仍严格受当前最终证据提交 CI 约束，执行结果由 Git tag 与 GitHub Actions 共同证明。
