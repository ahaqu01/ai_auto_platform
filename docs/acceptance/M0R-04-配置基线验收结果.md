# M0R-04 配置基线验收结果

> 执行日期：2026-08-21
> 基线提交：`fa8baef`
> 验收状态：`IMPLEMENTED_LOCAL`
> 结论：本地开发服务器验证通过；未经过独立评审、CI 和 Staging，因此不标记为 `ACCEPTED`。

## 1. 测试优先证据

实现前新增配置基线测试并执行：

```text
9 failed, 1 passed in 0.18s
```

失败原因分别为：Staging/Production 不拒绝缺失配置；Production 接受 loopback/localhost 和不安全公开端点；`.env.example` 缺少 `OIDC_AUDIENCE`；Compose 含 obsolete `version`。测试导入和本地配置用例正常，证明失败来自待实现能力。

实现后专项测试：

```text
11 passed in 0.13s
```

## 2. 验收标准核对

| 编号 | 结果 | 证据 |
|---|---|---|
| CFG-01 | 通过 | local 默认数据库和 Redis 指向 loopback，专项测试通过 |
| CFG-02 | 通过 | staging/production 缺少外部依赖字段时构造 Settings 立即失败 |
| CFG-03 | 通过 | loopback、localhost、本地默认数据库凭证被拒绝 |
| CFG-04 | 通过 | OIDC issuer 和 OSS public endpoint 在非本地环境强制 HTTPS |
| CFG-05 | 通过 | 异常只输出无效字段名，不输出连接串或密码值 |
| CFG-06 | 通过 | `.env.example` 使用 `OIDC_AUDIENCE` 并覆盖运行时字段 |
| CFG-07 | 通过 | 示例只含明确标注的本地值，无真实云凭证 |
| CFG-08 | 通过 | 删除 Compose `version`；实际 `docker compose config -q` 状态 0、stderr 为空 |
| CFG-09 | 通过 | PostgreSQL、Redis、MinIO 端口均只绑定 `127.0.0.1`，文件标注 local-only |
| CFG-10 | 通过 | 配置专项、API、迁移、Web、Agent 和 OpenAPI 回归通过 |

## 3. 全量验证

- Ruff format/check（API 源码、测试、脚本）：通过。
- API：`31 passed`，总覆盖率 `82%`；保留 1 条已登记的 TestClient 上游弃用警告。
- Alembic：`No new upgrade operations detected.`
- Compose：解析状态 0，stderr 为空。
- Production 缺配置启动保护：进程状态 1；异常只列 8 个无效字段名。
- npm audit：`0 vulnerabilities`。
- Web：`1 passed`；生产构建成功。
- Agent：`go vet ./...` 与 `go test -race ./...` 通过。
- OpenAPI：受控快照为 current。

## 4. 实现说明与边界

- Settings 在模型构造后自动执行部署环境校验，避免调用方漏调校验函数。
- Staging 与 Production 使用相同的外部依赖完备性和本地地址防误用门禁。
- 保留 `validate_production()` 兼容入口，但实际委托统一部署校验。
- `.env.example` 是本地开发模板，不承载 AccessKey、客户端密钥或生产密码。
- 本阶段没有引入真实 Keycloak、Temporal、OSS 凭证或 Secret Manager。

## 5. 后续门禁

进入 `ACCEPTED` 前仍需：

1. 在私有远端仓库完成独立评审；
2. CI 自动执行配置专项测试和 Compose 解析检查；
3. 用真实但不入库的 Staging 配置启动 API，并验证错误日志脱敏；
4. 部署/IaC 阶段接入 Secret Manager/KMS 与凭证轮换策略。
