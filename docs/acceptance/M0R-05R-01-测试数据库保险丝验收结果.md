# M0R-05R-01 测试数据库保险丝验收结果

> 执行日期：2026-08-24
> 基线：`6909d4c`
> 状态：`IMPLEMENTED_LOCAL`
> 实现提交：以包含本文档的 Git 提交为准

## 测试优先证据

实现前：`postgresql.safety` 不存在，安全测试收集失败：`1 error in 0.16s`。失败原因是保险丝能力尚未实现，不是数据库或 pytest 环境损坏。

实现后：

- 纯安全专项：`18 passed`。
- PostgreSQL 专项（专用数据库、显式 downgrade 确认）：`20 passed`。
- API 全量（包含安全和 PostgreSQL 专项）：`54 passed`，覆盖率 `82%`。

## 验收核对

| ID | 结果 | 证据 |
|---|---|---|
| DBG-01 | 通过 | 4 个错误确认值均在连接前拒绝 |
| DBG-02 | 通过 | 只允许 PostgreSQL，database/role 必须以 `_test` 结尾 |
| DBG-03 | 通过 | platform/prod/production/live 和 Production 大小写组合均拒绝 |
| DBG-04 | 通过 | DDL 前查询并核对 current_database/current_user |
| DBG-05 | 通过 | 连接角色 rolsuper=true 时拒绝；本地 platform_test 为 false |
| DBG-06 | 通过 | downgrade 使用独立确认常量和封装入口 |
| DBG-07 | 通过 | 反向测试确认错误不含密码或完整 URL |
| DBG-08 | 通过 | 本地 platform_test 数据库归 platform_test 非超级用户所有；platform 应用库不变 |
| DBG-09 | 通过 | schema 为 test_<uuid>，清理前重新核对数据库身份和角色权限 |
| DBG-10 | 通过 | Ruff、API、Alembic、Compose、Web、Agent、OpenAPI 回归通过 |

## 版本和变更边界

- 新增纯安全模块、反向测试、专用测试数据库初始化 SQL。
- fixture 在任何 DDL 前执行静态目标校验和运行时数据库身份校验。
- downgrade 只能通过二次确认封装调用。
- Compose 新数据卷自动初始化专用本地测试数据库；现有卷已幂等配置。
- 未修改业务数据库结构、API、应用库数据或生产配置。

## 全量验证

- Ruff format/check：通过。
- Alembic model check：无待生成迁移。
- Compose config：通过，无 stderr。
- npm audit：0 vulnerabilities；Web 1 passed，生产构建通过。
- Go vet/race：通过。
- OpenAPI snapshot：current。
- 保留 1 条已登记的 FastAPI/Starlette TestClient 上游弃用警告。

## 未关闭边界

- 本地测试数据库仍是长期存在的专用数据库，不等同于 CI 一次性数据库。
- CI/Staging 必须使用独立最小权限角色并在任务结束后销毁临时数据库。
- M0R-05R-02 将继续收紧 SQLSTATE、约束名和 base 对象清单。
- 未完成独立复审和 CI/Staging 证据，因此不得晋升为 ACCEPTED。
