# M2-CLOSE-03 风险豁免记录

- 日期：2026-09-16
- 决策人：项目负责人
- 决策：关闭 M2-CLOSE-03，并启动 M2-CLOSE-04。

## 事实

- 本项目目录与受限配置目录中的旧 Key 引用数为 0。
- 全部携带 OSS 配置的 Staging 容器均使用新 Key（仅记录尾号 `8cg4`）。
- 新 Key 已通过完整真实 OSS 浏览器 E2E，结果为 `1 passed (4.0m)`；测试后 Bucket 对象数为 0。
- 旧 Key 仍由本项目范围外的其他服务使用，当前不能停用或删除。

## 风险接受

项目负责人批准将外部服务的旧 Key 依赖迁移、停用和删除列为独立遗留安全风险，不再作为本项目 M2-CLOSE-03 的阻塞门槛。

该决定不证明旧 Key 安全，也不取消其后续迁移和废止责任。任何新项目、本项目新部署或本项目服务均不得重新使用旧 Key。

## 补偿控制

- 本项目仅使用新 Key。
- 凭据仅进入服务器 `0600` 受限配置，不进入 Git、日志或测试制品。
- Bucket 保持私有并阻止公共访问，CORS 使用精确来源。
- 保留真实 OSS E2E、容器配置审计、CI 和清理证据。
