# M1-07 项目完整生命周期验收记录

> 日期：2026-08-27
> 环境：本地服务器 / PostgreSQL / Docker Compose Demo
> 裁决：IMPLEMENTED_LOCAL / PASSED_LOCAL / BLOCKED_EXTERNAL

## 验收结论

项目 CRUD、归档/恢复、项目成员、分页和数据库级乐观锁已实现。权限隐藏、企业成员约束、软删除与旧版本失败语义均通过真实 PostgreSQL 验收。

## 自动化证据

- 专项：`3 passed`，覆盖完整生命周期/成员权限、opaque cursor 分页、同版本并发更新。
- PostgreSQL 全量：`50 passed in 11.06s`，包含迁移 upgrade/downgrade 与全部历史回归。
- 本地 CI：API `166 passed, 21 deselected`；Web `5 passed`；构建、Go、文档和部署契约通过。
- OpenAPI 快照与调用方身份输入安全策略通过。
- 同一版本并发更新结果为一个 200、一个 412，最终 version=2。
- 项目唯一冲突在 flush 阶段也稳定回滚并返回 409。

## Demo 证据

API 镜像重建后容器 healthy，迁移表版本为 `20260827_05`。本阶段未获得外部远端 CI 和 Staging 签章授权。

## 风险与后续门禁

M1-07 仍依赖应用层租户校验。M1-08 必须补齐审计、幂等和 Outbox；M1-09 必须补齐 RLS 与连接池上下文清理。在二者完成前不得进入资产/任务域。
