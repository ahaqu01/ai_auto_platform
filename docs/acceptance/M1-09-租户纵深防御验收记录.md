# M1-09 租户纵深防御验收记录

> 日期：2026-08-27
> 环境：本地开发依赖 / PostgreSQL 16 / Docker Compose Demo
> 结论：IMPLEMENTED_LOCAL / PASSED_LOCAL / BLOCKED_EXTERNAL

## 验收结论

应用 RBAC 与 PostgreSQL RLS 双层授权已落地。运行角色无超级用户、BYPASSRLS、CREATEROLE 和登录能力；7 张租户表启用 RLS；无租户上下文默认拒绝普通租户表；连接复用未发现 role 或 GUC 污染。

## 自动验证证据

- 专项测试：`2 passed in 1.56s`。
- PostgreSQL 全量：`56 passed in 19.04s`。
- 同一 backend PID 依次执行租户 A、租户 B、无租户事务；A/B 读取严格隔离。
- A 更新 B 资源影响 0 行；A 向 B 写入触发 RLS 拒绝。
- commit 与 rollback 后 `current_user` 恢复为连接用户，actor/organization GUC 为空。
- API 跨租户访问保持 404；组织、邀请、成员、项目及 M1-08 审计/幂等/Outbox 回归通过。
- 全迁移升降级与零残留目录契约通过。
- 本地 CI：API `166 passed, 27 deselected`；Web `5 passed`；OpenAPI、Web build、npm audit、Go vet/race、部署静态契约通过。

## Demo 证据

- API：healthy。
- `alembic_version`：`20260827_07`。
- 当前 schema 启用 RLS 的表：7。
- `platform_runtime` 由初始化脚本以 NOLOGIN/NOSUPERUSER/NOCREATEROLE/NOBYPASSRLS 配置；专项测试同时查询并断言四项属性为 false。

## 状态边界

本地与 Demo 证据不等于外部签收。远程 CI、PR 和外部 Staging 因缺少外部权限保持 `BLOCKED_EXTERNAL`。M1-08/M1-09 强制前置阻断已解除，仍需按计划完成 M1-10 和 M1 总体验收。
