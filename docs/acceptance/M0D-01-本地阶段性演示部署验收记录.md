# M0D-01 本地阶段性演示部署验收记录

## 1. 验收结论

**通过。**

当前阶段成果已部署到本地服务器，可从可信局域网通过 `http://192.168.1.129:8080` 访问。该结论仅适用于内部演示环境，不代表生产就绪，也不改变 REV-01 的整改结论和后续门禁。

## 2. 版本与环境

- 基线提交：`6c99c858ca95f7feddda9938a2f46e4aee1ba74e`
- 基线标签：`rev-01-remediation-required`
- 服务器：`192.168.1.129`
- 项目：`/home/diffgram/workspace/ai_auto_platform`
- Compose 项目：`ai-auto-platform-demo`
- 局域网入口：`http://192.168.1.129:8080`
- 计划标签：`m0d-01-local-demo-deployed`

## 3. 测试先行证据

实现前执行：

```text
FAIL: missing deploy/demo/docker-compose.yml
```

红灯准确识别演示部署入口缺失。实现后同一静态契约测试通过：

```text
PASS: M0D-01 static deployment contract
```

## 4. 验收结果

### AC-01 可重复部署：通过

- 已提供独立 Compose、API/Web 镜像构建文件、Nginx 配置、操作手册。
- `docker compose config` 和静态契约测试通过。
- API 使用服务器本地忽略目录 `.demo-cache/wheels` 离线构建，避免服务器公网不稳定；缓存不进入 Git。
- Web 镜像复制已通过 `npm run build` 验证的 `dist` 产物。

### AC-02 数据库初始化：通过

迁移容器退出码为 0，日志确认：

```text
Running upgrade  -> 20260820_01, initial identity and project tables
Running upgrade 20260820_01 -> 20260821_02, add stable OIDC identity columns
```

PostgreSQL 使用独立命名卷持久化。API 依赖迁移成功后才启动。

### AC-03 统一网页入口：通过

服务器本机 HTTP 冒烟测试：

```text
PASS: M0D-01 HTTP smoke test at http://127.0.0.1:8080
```

开发机经局域网直接访问结果：

```text
HomeStatus       : 200
HasPlatformTitle : True
HealthStatus     : ok
Service          : ai-auto-platform
Version          : 0.1.0
```

`/docs` 返回 Swagger UI。自动浏览器的 DOM/截图检查受桌面沙箱 ACL 初始化错误阻止，已用开发机局域网 HTTP 验证和 Web 单元测试替代，不计为平台失败。

### AC-04 最小暴露面：通过

- M0D-01 发布：`0.0.0.0:8080 -> web:80`。
- API 未发布宿主机端口。
- M0D-01 内的 PostgreSQL、Redis、MinIO 未发布宿主机端口。
- 宿主机已有的 `5432/6379/9000/9001` 均仅监听 `127.0.0.1`。
- 未修改路由器、公网安全组、公网 DNS。

### AC-05 可靠性：通过

最终服务状态：

- PostgreSQL：healthy
- Redis：healthy
- MinIO：healthy
- migrate：Exited (0)
- API：healthy
- Web：healthy

运行服务均配置 `restart: unless-stopped`，一次性迁移服务配置 `restart: "no"`。

### AC-06 自动化验证：通过

- API：`123 passed, 9 deselected, 1 warning`
- Web：`1 passed`
- Web 生产构建：成功，3186 modules transformed
- 静态部署契约：通过
- HTTP 冒烟：通过
- `git diff --check`：通过

首次 API 全量测试未提供 PostgreSQL 安全确认变量，得到 `123 passed, 9 setup errors`；这是数据库保险丝的预期拒绝。随后按本阶段约定使用 `-m 'not postgresql'` 正确执行非 PostgreSQL 回归，没有放松保险丝。

现存 1 条 Starlette/httpx 弃用警告与本任务无关，沿用既有技术债记录。

## 5. 已知边界

当前网页展示首页和 API 状态，不包含完整用户注册登录、资产、标注、训练、模型部署界面。API 的组织/项目接口已存在，但认证尚未接入演示 UI。REV-01 的两个 P1 和五个 P2 仍需按 V4.0 计划整改。

本环境使用明文 HTTP 和本地演示凭据，只允许可信局域网访问，禁止直接映射到公网。

## 6. 运维入口

```bash
cd /home/diffgram/workspace/ai_auto_platform
docker compose -f deploy/demo/docker-compose.yml ps
docker compose -f deploy/demo/docker-compose.yml logs --tail=200
DEMO_BASE_URL=http://127.0.0.1:8080 bash scripts/smoke_demo.sh
```

停止但保留数据：

```bash
docker compose -f deploy/demo/docker-compose.yml down
```

禁止在未备份时执行 `down -v`。
