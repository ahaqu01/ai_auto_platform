# M2-01 对象 Key 与威胁模型开发文档

> Owner：平台架构 / API / 安全 / QA
> 日期：2026-09-02
> 状态：DESIGN_ACCEPTED

## 1. 目标与边界

M2-01 固化资产与对象存储闭环的设计基线，不实现存储 SDK、上传接口、数据库迁移或 Web 页面。交付物覆盖双状态机、服务端对象 Key、完整性与配额边界、授权顺序、失败恢复、威胁模型和 M2-02～08 的验收约束。

## 2. 核心决策

1. 上传会话与资产是两个独立聚合，不共享一个含混状态字段。
2. 对象 Key 只能由服务端生成，格式为 `v1/o/{32位小写十六进制随机值}`；不包含组织、项目、用户、邮箱、文件名、日期或客户端路径。
3. Bucket 不由客户端选择。开发使用 MinIO；OSS Staging 固定为 `cn-hangzhou`、`https://oss-cn-hangzhou.aliyuncs.com`、私有 Bucket `aiautoplatform`。
4. 客户端 SHA-256 仅是预期值。对象只有经服务端完整流式 SHA-256 验证并通过安全门禁后才可进入 `AVAILABLE`。
5. 单文件契约上限为 20 GiB；1 GiB 是强制数据面 E2E 样本，不代表产品上限。
6. 授权检查先于资源状态和归档检查；跨租户继续返回 404，已授权主体对归档项目写入返回 `409 PROJECT_ARCHIVED`。
7. 数据库是业务状态源，对象存储是字节载体；所有跨系统中间态必须可由幂等命令或 Reconciler 收敛。

## 3. 状态机

上传会话：`PENDING_UPLOAD → UPLOADING → COMPLETING → COMPLETED`；可从活动状态进入 `ABORTED`、`EXPIRED` 或 `FAILED`。终态不可重新激活，重试创建新会话；重复完成只可返回原结果。

资产：会话完成后创建 `VERIFYING` 资产；验证通过进入 `AVAILABLE`，摘要、类型或恶意内容失败进入 `QUARANTINED`，不可恢复故障进入 `FAILED`；受控删除为 `AVAILABLE/FAILED/QUARANTINED → DELETING → DELETED`。`DELETED` 为业务终态，物理清理失败由对账继续处理。

## 4. 安全与恢复

威胁模型覆盖跨租户访问、路径/Key 注入、覆盖写、预签名泄漏与重放、超大文件、压缩炸弹、内容类型欺骗、摘要欺骗、并发完成、归档竞态、孤儿对象和日志泄密。预签名 URL、Upload ID、分片令牌、AccessKey 和临时凭据不得进入日志、审计 payload、异常文本或 Git。

恢复矩阵要求：DB 成功/OSS 失败保留可重试会话；OSS 成功/DB 失败由同 Key 幂等完成或对账接管；重复/并发完成只生成一个资产；删除失败保持 `DELETING`；对象缺失不得标记 `AVAILABLE`。

## 5. 阿里云 OSS 前置条件

M2-01 不使用长期 AccessKey。M2-02 前应完成长期凭据轮换，并建立最小权限 RAM/STS 身份、受控密钥注入、Bucket 私有访问、CORS、生命周期、版本策略和地域校验。MinIO 结果不得替代真实 OSS Staging E2E。

## 6. 交付物

- `docs/adr/0006-asset-object-identity-and-lifecycle.md`
- `docs/design/06-M2资产与对象存储设计基线.md`
- `docs/acceptance/M2-01-对象Key与威胁模型-验收标准.md`
- `docs/acceptance/M2-01-对象Key与威胁模型-验收记录.md`
- 本开发文档及权威状态更新

## 7. 验收结果

- 设计冲突已裁决：Key、状态机、SHA-256、病毒扫描门禁、20 GiB 上限和归档语义均有唯一口径。
- 威胁模型、失败恢复和测试矩阵已覆盖 M2 后续工作包。
- 文档检查和完整 CI 作为本提交的发布门禁；结果记录在验收记录和对应 CI run。
- 本工作包没有写入密钥、实现上传接口或提前宣称 OSS 兼容性通过。

## 8. 版本管理

- 基线：`3a23227` / `codex/m2-assets-storage`
- 提交目标：`docs: accept M2-01 asset storage design`
- M2-02 只能从本设计门禁通过后的提交开始。
