# M2-06 资产访问与授权开发文档

> Owner：平台 API / 授权 / 存储 / 安全 / QA  
> 日期：2026-09-11  
> 状态：历史 M2-06 ACCEPTED；2026-09-17 P2 下载合同功能修订通过专项验收（不代表 STS 安全项关闭）

## 1. 范围

M2-06 实现项目资产列表、资产详情、短期下载 URL 和软删除请求，覆盖组织/项目可见性、角色、资产状态和归档状态。不实现 M2-07 的物理对象删除、后台重试、过期清理或 Reconciler。

## 2. API

- `GET .../projects/{projectId}/artifacts`：项目成员可见，按创建时间和 ID 倒序游标分页，默认 50、最大 100；DELETING/DELETED 不出现在列表中。
- `GET .../artifacts/{id}`：返回资产元数据和 ETag；删除中及已删除资产按 404 隐藏。
- `POST .../artifacts/{id}:download-url`：仅 AVAILABLE 资产可签发，默认 600 秒，允许 60～900 秒（2026-09-17 P2 复评修订，大于 900 返回 422）；QUARANTINED、VERIFYING、FAILED、DELETING、DELETED 均返回 `ARTIFACT_NOT_AVAILABLE` 或隐藏。
- `DELETE .../artifacts/{id}`：需要项目写权限、活动项目、Idempotency-Key 和 If-Match；只转为 DELETING 并产生删除请求事件，物理删除由 M2-07 收敛。

公开 DTO 不包含 `object_key`、`bucket_alias`、远端 Upload ID 或凭据。

## 3. 授权语义

授权顺序为身份、组织成员、项目可见性、项目角色、资产状态。跨租户或非项目成员统一使用 404，防止确认资产存在。组织 Owner/Admin 及项目 Admin/Engineer 可请求删除；普通项目成员可以读取和下载 AVAILABLE 资产。归档项目允许只读，但删除返回 `409 PROJECT_ARCHIVED`。

## 4. 下载安全

下载签名仅在服务端授权完成后调用对象存储端口。下载名移除路径信息、反斜线和双引号，限制为 255 字符；返回 RFC 5987 `Content-Disposition` 建议值。签名 URL 只出现在响应中，审计、Outbox 和日志不保存 URL。

## 5. 删除一致性

删除通过资产版本 ETag 防止并发覆盖，通过 Idempotency-Key 防止重复事件。状态切换、审计和 `ArtifactDeletionRequested.v1` Outbox 在同一数据库事务提交。DELETING 立即失去业务可见性和下载资格；即使后续物理删除失败，也不会恢复为 AVAILABLE。

## 6. 验收

专项矩阵覆盖列表/详情、响应字段最小化、ETag、AVAILABLE 下载、安全文件名、短 TTL、隔离资产拒绝、跨租户 404、删除幂等/版本控制、删除后隐藏以及归档项目读写分离。最终提交号、全量与 PostgreSQL/CI 证据记录在配套验收记录中。
