# 账号自助注册验收记录

验收日期：2026-09-15

结论：通过（可信局域网 Demo 范围）。

## 证据

- 定向 API：5 passed，覆盖注册跳转、恶意返回地址、回调、HttpOnly 会话、state 重放和真实 PKCE 参数。
- 完整 API：245 passed，28 deselected；被排除项为项目既有 PostgreSQL 分组。
- Web：7 个测试文件、17 项测试全部通过；生产构建通过。
- Ruff、OpenAPI 快照、文档检查、Demo 部署契约、npm high 门禁及 Go `test -race` 均通过。
- 运行时 `GET /auth/register?return_to=/workspace` 返回 303，目标为局域网 Keycloak registrations endpoint，回调为 `http://192.168.1.129:8080/auth/callback`。
- Keycloak 运行时配置确认：注册开启、邮箱作用户名、邮箱唯一、暴力破解保护和复杂密码策略开启。
- Keycloak 运行时国际化配置确认：启用国际化，唯一支持语言及默认语言均为 `zh-CN`；登录页与注册页返回中文文案。
- API、Web、Keycloak、PostgreSQL、Redis、MinIO 容器均为 healthy。

## 已知限制

- Demo 没有 SMTP，邮箱真实性不验证；不得直接照搬到公网生产环境。
- npm audit 报告 2 个 moderate 的 Vitest 开发依赖问题，不触发项目既定 high 门禁，且不进入生产 bundle；后续依赖升级处理。
