# M1-09 租户纵深防御开发文档

> Owner：平台后端 / 安全
> 日期：2026-08-27
> 状态：IMPLEMENTED_LOCAL / PASSED_LOCAL / BLOCKED_EXTERNAL

## 1. 目标与边界

M1-09 在应用 RBAC 之外增加 PostgreSQL RLS 纵深防御，防止查询条件遗漏导致跨租户读取或写入。RLS 不替代成员、角色和资源状态检查；`users` 与 `idempotency_records` 仍为全局身份/基础设施表。

## 2. 运行角色与部署

- PostgreSQL 初始化创建 `platform_runtime`：`NOLOGIN`、`NOSUPERUSER`、`NOCREATEROLE`、`NOBYPASSRLS`。
- 数据库 owner 只负责迁移；API 每个认证事务使用 `SET LOCAL ROLE platform_runtime`。
- Alembic 不创建全局角色，只校验角色及成员关系并 fail-fast；角色配置属于数据库初始化职责。
- Local、Demo、Staging Compose 均挂载 `deploy/postgres/01-runtime-role.sql`，已有数据卷需先幂等执行该脚本。

## 3. 租户上下文

认证同步在 owner 权限下原子完成并提交，随后开启新事务：

1. `SET LOCAL ROLE platform_runtime`；
2. `set_config('app.actor_id', ..., true)`；
3. 访问租户资源前设置 `app.organization_id`；
4. commit、rollback 或连接归还后，LOCAL role 与两个 GUC 自动恢复。

创建组织先生成 UUID，再设置新组织上下文后写入。邀请接受通过只返回 organization ID 的窄 `SECURITY DEFINER` 函数解析 token，随后立即进入租户上下文。

## 4. RLS 覆盖

迁移 head `20260827_07` 为以下 7 张表启用 RLS：

- `organizations`
- `organization_members`
- `organization_invites`
- `projects`
- `project_members`
- `audit_events`
- `outbox_events`

普通租户表在无 organization context 时读 0 行、写入拒绝。组织列表仅允许 actor 依据自身 membership 查询；应用层继续返回既有 403/404 语义。

## 5. 验收结果

- M1-09 PostgreSQL 专项：`2 passed`。
- PostgreSQL 全量（含一次性数据库全升全降和零残留目录）：`56 passed`。
- 本地 CI：API `166 passed, 27 deselected`；OpenAPI、部署静态契约、Web `5 passed` 与 build、npm 高危审计、Go vet/race 全部通过。
- Demo API healthy；数据库 head 为 `20260827_07`；当前 schema 中 7 张表启用 RLS。
- 远程 CI、PR 和外部 Staging 签收因缺少外部权限仍为 `BLOCKED_EXTERNAL`，不标记 ACCEPTED。

## 6. 版本管理

- 基线：`48b2250` / `m1-08-audit-idempotency-outbox-implemented`
- 提交信息：`feat: implement M1-09 tenant RLS isolation`
- 标签：`m1-09-tenant-rls-implemented`

## 7. 后续计划

M1-08 与 M1-09 的资产/任务域强制前置条件已完成。下一阶段按计划执行 M1-10 Web 组织/项目闭环；M1 总体验收完成后再进入资产/任务域。
