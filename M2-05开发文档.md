# M2-05 完成与强校验开发文档

> Owner：平台 API / 数据库 / 存储 / 安全 / QA  
> 日期：2026-09-11  
> 状态：IMPLEMENTED / ACCEPTANCE PENDING CI

## 1. 范围

M2-05 实现 Multipart 完成、分片清单强校验、对象 Head、完整流式 SHA-256、大小校验、最终资产落库、隔离状态和两阶段失败恢复。不实现 M2-06 的资产列表、详情、下载授权或删除。

## 2. 完成接口

`POST .../upload-sessions/{id}:complete` 要求合法 `Idempotency-Key` 和按分片号升序的完整清单。服务端将清单与数据库登记记录逐项比较，并验证登记总大小；缺失、重复、乱序、ETag 不一致或总大小不完整均在调用存储前返回 `PART_LIST_MISMATCH`。

完成过程先将会话提交为 `COMPLETING`，再执行外部存储调用，避免在持有业务行锁的事务中访问网络。重试可从 `COMPLETING` 恢复；若 Complete 返回冲突或不确定失败但 Head 已发现对象，则按“存储已成功、DB 尚未完成”继续验证。最终通过 `upload_session_id` 和 `object_key` 唯一约束只创建一个资产。

## 3. 强校验与状态

验证器先 Head 对象，再通过内部读取通道分块读取全部字节并计算 SHA-256；Multipart ETag、客户端摘要及抽样均不作为可信摘要。

- 大小和 SHA-256 均一致：资产 `AVAILABLE`、完整性 `VERIFIED`。
- 大小或 SHA-256 不一致：资产 `QUARANTINED`、完整性 `MISMATCH`，分别返回 `SIZE_MISMATCH` 或 `CHECKSUM_MISMATCH`。
- 对象缺失：返回 `OBJECT_MISSING`，会话保留 `COMPLETING` 供恢复任务或重试处理。
- 存储暂时不可用：返回 `STORAGE_UNAVAILABLE`，不会错误创建资产。

成功或隔离后上传会话转为 `COMPLETED`、释放预约字节并记录完成时间。完成动作、资产状态、大小和完整性结论写入审计和 Outbox，不记录对象 Key、Upload ID、摘要原文或凭据。

## 4. 数据库

迁移 `20260911_10` 新增 `artifacts`，包含租户/项目归属、上传会话、opaque Key、展示信息、声明/验证摘要、内容类型、完整性/扫描/资产状态和版本。表启用 FORCE RLS；`upload_session_id` 与 `object_key` 均唯一。

## 5. 存储边界

M2-02 存储端口增加分块读取能力；适配器在流中持续检查取消信号并只记录 Key 哈希引用。真实 OSS/MinIO 数据面仍受既有 SSRF/DNS/重定向/对端/TLS 安全门约束，本工作包不注入长期密钥，也不以协议替身冒充 Staging E2E。

## 6. 验收

专项矩阵覆盖成功完成、完整流式摘要、终态重放、不完整清单前置拒绝、大小不一致、摘要不一致、隔离落库和预约释放。最终提交、PostgreSQL/RLS、全量测试和 GitHub Actions 证据记录在配套验收记录中。
