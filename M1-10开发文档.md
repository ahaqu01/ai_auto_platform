# M1-10 Web 组织/项目闭环开发文档

> Owner：Web / 平台后端
> 日期：2026-08-27
> 状态：IMPLEMENTED_LOCAL / PASSED_LOCAL / BLOCKED_EXTERNAL

## 1. 目标与边界

M1-10 将 M1-02 至 M1-09 已完成的身份、组织、成员、项目和租户隔离能力接入管理台。范围仅包含登录/退出入口、组织切换、成员管理和项目管理，不进入资产、任务、训练或设备域。

## 2. 页面与交互

- 新增 `/workspace` 组织与项目管理页，并从主导航直接进入。
- 登录与退出继续复用 BFF Session Cookie；所有 API 请求使用 same-origin credentials。
- 当前组织可切换并保存到 localStorage；切换后并行刷新成员和项目。
- 支持新建组织、邀请成员、修改成员角色和移除成员。
- 支持创建项目、修改项目名称、归档和恢复项目。
- 页面展示 API Problem Details 的 code/detail，不把授权失败伪装为成功。

## 3. 并发与安全协议

- 写请求由 `authenticatedFetch` 自动附带 BFF CSRF token。
- 组织和项目创建生成独立 `Idempotency-Key`。
- 项目更新、归档和恢复使用当前 version 生成 `If-Match`，412 冲突原样反馈并提示刷新。
- 成员 Owner 操作在 UI 禁用，最终权限和最后 Owner 约束仍由 API 强制执行。

## 4. 验收结果

- Web：`9 passed`，覆盖既有登录/工作台及新增 API 客户端和管理页。
- `vue-tsc -b` 与 Vite production build 通过。
- OpenAPI 快照、API 回归、文档治理、部署契约、npm audit 和 Go 检查通过。
- Demo Web 重建后 `/workspace` SPA 路由可访问，Web/API healthy。
- 远程 CI、PR 和外部 Staging 签收缺少外部权限，保持 `BLOCKED_EXTERNAL`。

## 5. 版本管理

- 基线：`8d7c821` / `m1-09-tenant-rls-implemented`
- 提交信息：`feat: implement M1-10 web tenant workspace`
- 标签：`m1-10-web-tenant-workspace-implemented`

## 6. 后续计划

M1-01 至 M1-10 的本地实现链路完成。下一步应执行 M1 总体验收，重点补真实 Keycloak 浏览器 E2E、完整身份/授权矩阵、远程 CI 与 Staging 签收；通过总体验收后再进入资产/任务域。
