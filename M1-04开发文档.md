# M1-04 开发文档

> Owner：平台工程
> 日期：2026-08-27
> 状态：IMPLEMENTED_LOCAL
> 前置版本：bc0d410（m1-03-atomic-identity-implemented）

## 目标与边界

M1-04 固定 DTO、错误响应和 OpenAPI 契约，为后续成员、RBAC 与项目生命周期提供稳定基础。本阶段没有新增业务能力，也没有提前实现 M1-05 及后续工作包。

## 严格 DTO

新增公共契约基类 StrictModel 和 StrictOrmModel，统一配置 extra=forbid。组织、项目和健康响应 DTO 已迁移到严格基类：

- 组织或项目创建出现未知字段时返回 422；
- Pydantic 错误类型固定为 extra_forbidden；
- OpenAPI 的 OrganizationCreate、ProjectCreate、ProblemDetails 固定 additionalProperties=false；
- ORM 响应继续支持 from_attributes，不改变既有序列化行为。

## Problem Details 与认证挑战

新增 ProblemDetails 模型，固定字段：

- type
- title
- status
- code
- detail
- instance
- traceId

DomainError 全局处理器使用该模型生成响应。401 自动增加 WWW-Authenticate: Bearer；403、404、409 不附加该头。现有错误码和中文安全提示保持不变。

## OpenAPI 基线

组织和项目受保护路由统一声明 401、403、404、409，响应体引用 ProblemDetails。401 同时声明 WWW-Authenticate 响应头。浏览器退出端点声明 401 和 403。

受控快照 packages/contracts/openapi.json 已通过导出脚本更新；字节稳定性、调用者身份不得作为输入、operationId 唯一性等既有门禁继续通过。

## 测试与验收

红灯基线：

- 4 failed、3 passed；
- 缺口分别是 DTO 忽略额外字段、401 无 challenge、OpenAPI 缺少错误声明。

实现后：

- M1-04 专项：8 passed；
- OpenAPI 与专项组合：17 passed；
- CI baseline：API 166 passed、14 deselected；
- PostgreSQL 全量与迁移往返：43 passed；
- Web：5 passed，构建成功；
- npm audit：0 vulnerabilities；
- Go：go test ./... 通过；
- 文档治理：DOC CHECK PASSED；
- Demo：API healthy，live/ready 200，401 challenge 与运行时 OpenAPI 标记通过。

## 安全复核

- 响应只使用 DomainError.safe_detail，不回传内部异常；
- 401 challenge 值固定为 Bearer，不拼接用户输入；
- 403/404/409 不携带误导性认证头；
- 租户隐藏语义的 404 保持不变；
- 严格 DTO 阻止静默接受未来或拼写错误字段。

## 后续计划

下一阶段执行 M1-05 企业成员邀请与 RBAC。M1-06 负责最后 Owner 并发保护；进入资产/任务域前仍须完成 M1-08 与 M1-09。
