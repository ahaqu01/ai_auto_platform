# M2-02 对象存储适配端口开发文档

> Owner：平台 API / 安全 / QA
> 日期：2026-09-02
> 状态：IMPLEMENTED_LOCAL / ACCEPTANCE_PENDING_CI

## 1. 目标与边界

本工作包实现 M2-01 定义的厂商无关对象存储端口，以及 MinIO/阿里云 OSS 的 S3 兼容适配契约。范围包括 Multipart、Head、下载签名、删除、错误归一化、超时、重试、取消和日志脱敏。

M2-02 不实现上传会话数据库/API，也不绕过 M0R-04R-05 的运行时 SSRF 门禁。真实 SDK/HTTP gateway、凭据注入和真实 MinIO/OSS 网络 E2E 必须在满足固定 peer 与重定向约束后接线。

## 2. 实现

- `ObjectStoragePort`：向领域层提供稳定异步端口。
- `S3CompatibleGateway`：隔离 SDK/transport 的安全边界，原始 SDK 异常不得越界。
- `S3CompatibleObjectStorageAdapter`：统一操作编排、超时、有限指数退避和错误归一化。
- `MinioObjectStorageAdapter` 与 `AliyunOssObjectStorageAdapter`：固定 provider 配置契约。
- `PresignedRequest`：repr 永久脱敏，避免交互式日志泄露 URL。
- 日志只包含 provider、操作名、Key 哈希引用、重试次数和归一化错误码。

## 3. 错误与重试

统一错误码为 NOT_FOUND、CONFLICT、ACCESS_DENIED、INVALID_REQUEST、TIMEOUT、TEMPORARY_UNAVAILABLE、CANCELLED 和 INTERNAL。仅 TIMEOUT 与 TEMPORARY_UNAVAILABLE 自动重试；权限、参数、冲突和不存在不重试。所有外部调用都有请求超时，取消信号可在调用前或退避期间终止操作。

## 4. 安全边界

对象 Key、Upload ID、预签名 URL、AccessKey、Secret 和 SDK 原始异常不得进入日志或向上层暴露。Gateway 实现必须在真实 transport 边界满足 DNS 解析、IP 验证、socket peer 固定、TLS SNI/证书和逐跳重定向校验，才能解除网络客户端阻断。

## 5. 验收

自动化测试覆盖完整端口调用、provider 校验、临时/永久错误、超时、有限重试、调用前取消、退避期取消、非法 part number 和预签名 repr 脱敏。旧 OSS 安全测试已原子更新，继续禁止厂商 SDK 依赖或直接引用。

## 6. 版本管理

- 基线：`269f87e`
- 工作分支：`codex/m2-assets-storage`
- 目标提交：`feat: add M2-02 object storage port`
- 本工作包通过 CI 后追加验收证据，不改写已推送历史。

## 7. 后续

M2-03 只能依赖 ObjectStoragePort，不得直接依赖 S3CompatibleGateway 或厂商 SDK。真实 OSS 联调前必须轮换已暴露的长期凭据，并使用最小权限 RAM/STS 与受控密钥注入。
