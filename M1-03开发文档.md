# M1-03 开发文档

> Owner：平台工程
> 日期：2026-08-27
> 状态：IMPLEMENTED_LOCAL
> 前置版本：dd6e112（m1-02-bff-session-implemented）

## 目标与结论

M1-03 已落实 ADR-0003：用户主身份只由 (external_issuer, external_subject) 确定，邮箱降级为普通资料字段。相同邮箱、不同 OIDC subject 创建独立用户，不自动合并；同一身份的并发首次登录通过 PostgreSQL 原子 upsert 收敛为一行。

本地实现和 Demo 验收通过。远端推送、PR 和外部 Staging 签章不在当前凭据范围内，仍按 BLOCKED_EXTERNAL 管理。

## 数据库变更

新增 Alembic revision 20260827_03：

- upgrade 删除 users.uq_users_email；
- downgrade 恢复 uq_users_email(email)；
- 保留 uq_users_external_identity(external_issuer, external_subject)；
- ORM 模型同步移除 email 的 unique=True。

迁移已在带 owner/marker 保护的一次性 PostgreSQL 数据库完成 upgrade、downgrade、upgrade 往返；Demo 数据库已升级到 20260827_03。

## 原子身份同步

_synchronize_user 不再执行“先 SELECT、再 INSERT”的竞态流程。PostgreSQL 和测试使用的 SQLite 均执行单条 INSERT ... ON CONFLICT ... DO UPDATE ... RETURNING。

行为约束：

1. 同 issuer/subject 始终返回同一用户；
2. 不以邮箱查询或合并用户；
3. OIDC 提供非空邮箱、显示名时更新资料；
4. OIDC 缺失邮箱或给出空显示名时保留已有值；
5. 非预期完整性冲突回滚事务并返回 IDENTITY_CONFLICT。

## 测试与验收

新增真实 PostgreSQL 用例覆盖：约束目录、同邮箱异主体、同一身份 20 路并发、非空资料更新及空值保护。

- 红灯基线：3 failed；
- M1-03 专项：4 passed；
- PostgreSQL 全量：43 passed；
- 迁移往返：3 passed；
- API 非 PostgreSQL：158 passed, 14 deselected；
- CI baseline：通过；
- Web：5 passed，构建通过，npm audit 0 vulnerabilities；
- Go：go test ./... 通过；
- 文档治理：DOC CHECK PASSED；
- Demo：API healthy，live/ready 200，数据库 head 20260827_03。

验收材料：

- docs/acceptance/M1-03-邮箱非唯一与原子身份同步验收标准.md
- docs/acceptance/M1-03-邮箱非唯一与原子身份同步验收记录.md

## 后续计划

按总计划进入 M1-04。进入资产/任务域前，M1-08（RLS）与 M1-09（审计、幂等、Outbox）必须完成并通过真实 PostgreSQL 验收。
