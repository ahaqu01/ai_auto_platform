# 账号自助注册开发文档

## 目标与范围

在现有 Keycloak + BFF 登录体系中增加自助注册。平台不保存用户密码；注册回调继续使用 OIDC Authorization Code、PKCE S256、nonce、一次性 state、HttpOnly 会话和 CSRF 防护。

本次仅对可信局域网演示环境开放注册。新账号只创建身份，不自动加入组织、项目或获得业务数据权限。Staging/Production 在配置邮件验证、反滥用和运维策略前保持关闭。

## 实现

- Web 顶栏增加“创建账号”，保留“登录”。
- BFF 新增 `GET /auth/register`，过滤外部返回地址。
- 注册跳转 Keycloak registrations endpoint，并复用安全登录事务及 `/auth/callback`。
- Demo Realm 启用邮箱用户名、唯一邮箱、暴力破解保护和 12 位复杂密码策略。
- Nginx 精确代理 `/auth/register` 到 API，其他 `/auth/` 请求仍交给 Keycloak。
- OpenAPI 受控快照同步更新。

## 版本

- `fcd77e7` — 注册 BFF、Web 入口、Realm 配置、测试与契约。
- `1046cd4` — 修复注册 BFF 的 Nginx 精确路由。

## 安全边界

- Demo 未配置 SMTP，因此未开启邮箱验证，仅适用于可信局域网演示。
- 新注册用户默认没有组织成员关系；必须创建组织或由组织管理员邀请后，才能访问相应租户资源。
- 不把 Access Token 暴露给浏览器 JavaScript。

## 使用

访问 `http://192.168.1.129:8080/`，点击“创建账号”，按密码策略填写信息。注册完成后由 Keycloak 回调平台并建立登录会话。
