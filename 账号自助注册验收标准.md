# 账号自助注册验收标准

1. 未登录界面同时显示“创建账号”和“登录”。
2. `/auth/register` 返回 303 并跳转到 Keycloak registrations endpoint。
3. 注册事务包含 Authorization Code、PKCE S256、nonce、一次性 state 和平台回调地址。
4. `return_to` 仅接受站内绝对路径，拒绝外部 URL 与协议相对 URL。
5. 回调建立 HttpOnly、SameSite=Lax 会话；state 重放返回 400。
6. Demo 启用唯一邮箱、暴力破解保护、12 位且包含数字/大小写/特殊字符的密码策略。
7. 新账号不自动获得任何组织或项目权限。
8. API、Web、文档检查、部署契约、构建与 Go race 测试全部通过。
9. 指定演示地址可返回有效注册跳转，相关容器健康。
10. Keycloak 登录页、注册页及认证提示默认显示简体中文，不提供非中文语言切换。
