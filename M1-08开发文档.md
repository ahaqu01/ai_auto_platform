# M1-08 审计、幂等与 Outbox 开发文档

> Owner：平台工程
> 日期：2026-08-27
> 状态：IMPLEMENTED_LOCAL / PASSED_LOCAL / BLOCKED_EXTERNAL

## 1. 目标与边界

M1-08 为关键写操作建立审计、请求幂等和 Transactional Outbox 基线，保证业务状态与证据/事件同事务提交。M1-09 的 PostgreSQL RLS 和连接池租户上下文不在本阶段实现。

## 2. 数据模型

- `audit_events`：主体、动作、资源、结果、企业/项目范围、trace ID 和脱敏 detail。
- `idempotency_records`：以 `(actor_id, route_key, idempotency_key)` 为主键，保存请求 SHA-256、处理状态、响应快照和 24 小时过期时间。
- `outbox_events`：event ID、企业、聚合、事件类型、版本化 payload、发生/发布时间和发布尝试次数。
- Alembic head：`20260827_06`。

## 3. 幂等协议

企业创建和项目创建要求 `Idempotency-Key`，长度 16–128：

1. 对规范化 JSON 请求计算 SHA-256。
2. 原子插入 PROCESSING 记录，数据库唯一键负责跨进程串行化。
3. 同键同摘要且已完成时返回首次状态码与响应快照，并返回 `Idempotent-Replayed: true`。
4. 同键不同摘要返回 `409 IDEMPOTENCY_CONFLICT`。
5. 缺少键返回 `428 IDEMPOTENCY_KEY_REQUIRED`。
6. 业务失败时幂等记录随事务回滚，允许使用相同或新键安全重试。

## 4. 审计与 Outbox

以下业务变更在同一数据库事务写入 SUCCESS 审计和 Outbox：

- 企业创建：`organization.create` / `OrganizationCreated.v1`
- 项目创建：`project.create` / `ProjectCreated.v1`
- 项目归档与恢复：`project.archive|restore` / `ProjectArchived|Restored.v1`
- 企业成员移除：`organization.member.remove` / `OrganizationMemberRemoved.v1`

事件 payload 包含 schemaVersion、aggregateVersion、traceId 和 data，不保存 Token、密码、Cookie 或 Idempotency-Key 原文。`published_at` 和 `attempts` 为后续至少一次发布器提供持久化状态；当前阶段不伪造外部发布成功。

## 5. 事务保证

业务记录、审计、Outbox 和幂等完成快照在一次 commit 中提交。唯一约束、乐观锁或其他失败触发 rollback 时四类数据共同回滚，不产生业务成功但事件缺失，或事件存在但业务失败的半状态。

## 6. 验收结果

- M1-08 PostgreSQL 专项：4 passed。
- PostgreSQL 全量：54 passed。
- 本地 CI：API 166 passed、25 deselected；Web 5 passed；Web build、Go、文档和部署契约通过。
- OpenAPI 快照与身份输入安全策略通过。
- Demo 迁移、镜像和健康验收见验收记录。
- 外部远端 CI、PR 和 Staging 签章未获授权，因此不标记 ACCEPTED。

## 7. 版本管理

- 基线：`6e9a724` / `m1-07-project-lifecycle-implemented`
- 提交信息：`feat: implement M1-08 audit idempotency and outbox`
- 标签：`m1-08-audit-idempotency-outbox-implemented`

## 8. 后续计划

下一阶段强制执行 M1-09 租户纵深防御，包括 RLS、连接池上下文设置/清理和跨企业读写矩阵。M1-09 通过前不得进入资产/任务域。
