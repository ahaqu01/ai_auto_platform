# M1 总体验收报告

> Owner：交付负责人 / 技术 / 安全 / QA
> 验收日期：2026-08-27
> 验收基线：`da08c1b` / `m1-10-web-tenant-workspace-implemented`
> 数据库版本：`20260827_07`
> 结论：**NOT ACCEPTED — TESTS_FAILED / BLOCKED_EXTERNAL**

## 1. 验收范围

本次覆盖 M1-PRE-01、M1-01 至 M1-10：Keycloak Realm IaC、BFF 会话、身份同步、API 契约、组织成员 RBAC、最后 Owner、项目生命周期、审计/幂等/Outbox、RLS 租户隔离和 Web 组织/项目闭环。

本报告执行总体验收，不新增业务能力。总门禁来自 `docs/plans/里程碑后开发评审与验收计划.md` 4.4；任一强制项失败或阻塞时不得进入 M2。

## 2. 总门禁结论

| ID | 门禁 | 结果 | 证据或差距 |
|---|---|---|---|
| M1-G01 | 真实 Keycloak 浏览器到 API E2E | BLOCKED | discovery、签名 service token、token→受保护 API 已通过；Realm IaC 中用户数和密码凭据数均为 0，无法执行真实用户 Authorization Code→BFF Cookie→Web→API E2E |
| M1-G02 | 身份/授权测试在 CI 和 Staging 通过 | BLOCKED | 本地全量通过；本机 Staging 通用 smoke 通过，但没有当前 M1 构建的远程 CI 与外部 Staging 身份/授权矩阵签收 |
| M1-G03 | auth/authorization/tenant 分支覆盖率 ≥90%，后端总体 ≥80% | FAIL | 193 tests 通过，但后端分支覆盖率 75%；关键文件为 auth/bff 57%、auth/dependencies 82%、auth/verifier 87%、organization_access 77%、organizations 54%、tenancy 78% |
| M1-G04 | Critical/High SCA/SAST 为 0或有批准例外 | BLOCKED | npm audit 为 0；服务器没有 pip-audit、Bandit、govulncheck，Python/Go SCA/SAST 无可审计结果，也没有例外审批 |
| M1-G05 | API、审计、数据库证明跨租户访问为 0 | PASS_LOCAL | M1-09 同连接 A/B/无租户矩阵、跨租户读写拒绝、commit/rollback 清理和 7 表 RLS 已通过；M1-08 审计/Outbox 同事务通过 |
| M1-G06 | 产品、技术、安全、QA 四方签字 | BLOCKED | 未取得四方签字 |

**决策：M1 不满足 ACCEPTED 条件，禁止进入 M2。**

## 3. 自动化证据

### 3.1 API、数据库与覆盖率

- 全量 API + PostgreSQL：`193 passed`。
- 包含随机隔离 schema、一次性带 owner/marker 数据库、全迁移升级/降级和目录零残留检查。
- 后端总体 branch coverage：`75%`，低于 `80%` 门槛。
- 覆盖率主要缺口集中在 BFF 异常/刷新路径、组织邀请和成员错误矩阵、项目分页/冲突/成员矩阵、RLS SQLite/邀请解析分支。

### 3.2 Web 与契约

- Web：`5 test files / 9 tests passed`。
- TypeScript 检查和 Vite production build 通过。
- OpenAPI 快照、文档治理、Demo 部署静态契约、Go vet/race 通过。

### 3.3 Keycloak 与运行环境

- Keycloak discovery 和签名 token：PASS。
- 真实 Keycloak client-credentials token 调用受保护 `/api/v1/organizations`：HTTP 200。
- Realm IaC：`realm_users=0`、`enabled_users=0`、`password_credentials=0`。
- Demo HTTP smoke：PASS；Demo API/Web/Keycloak/PostgreSQL/Redis/MinIO healthy。
- 本机 Staging HTTP/readiness smoke：PASS；不等同于当前 M1 外部签收。

### 3.4 供应链与静态安全

- Web `npm audit --audit-level=high`：0 vulnerabilities。
- Python `pip-audit`：工具不可用，BLOCKED。
- Python Bandit：工具不可用，BLOCKED。
- Go govulncheck：工具不可用，BLOCKED。

## 4. 缺陷与阻断清单

| ID | 等级 | 状态 | 问题 | 关闭条件 |
|---|---|---|---|---|
| M1-A01 | P1 | OPEN | 后端总体和关键模块 branch coverage 未达门槛 | 补齐关键异常/权限/并发测试；总体 ≥80%，关键模块聚合 ≥90% |
| M1-A02 | P1 | BLOCKED | 无可重复测试用户，真实浏览器 BFF E2E 无法执行 | 提供仅限 Demo/Staging 的非生产测试用户或自动创建/销毁夹具，完成浏览器 E2E |
| M1-A03 | P1 | BLOCKED_EXTERNAL | 当前 M1 构建缺远程 CI 和外部 Staging 身份/授权矩阵 | 远程 CI 通过并在 Staging 执行完整矩阵，保存构建/镜像摘要 |
| M1-A04 | P1 | BLOCKED_EXTERNAL | 产品、技术、安全、QA 未签字 | 四方基于同一证据包签字 |
| M1-A05 | P2 | OPEN | Python/Go SCA/SAST 证据缺失 | 在锁定版本的 CI 工具链运行 pip-audit、Bandit、govulncheck；High/Critical 清零或批准例外 |
| M1-A06 | P3 | OPEN | Starlette TestClient/httpx 兼容层产生弃用警告 | 固定兼容版本或迁移测试客户端，保持零警告 |

## 5. 已完成能力追溯

M1-01 至 M1-10 均有独立 implemented 标签，提交链从 `53217d3` 到 `da08c1b`。这些标签证明工作包本地实现和局部验收完成，不代表 M1 总门禁 ACCEPTED。

## 6. 推荐整改顺序

1. `M1-AC-01`：覆盖率补强，优先 BFF、authorization、tenant 和错误矩阵。
2. `M1-AC-02`：可重复测试用户夹具与真实浏览器 Keycloak/BFF E2E。
3. `M1-AC-03`：补齐 Python/Go SCA/SAST 固定工具链。
4. `M1-AC-04`：远程 CI 和当前 M1 镜像的 Staging 全矩阵。
5. `M1-AC-05`：产品、技术、安全、QA 四方复核签字；全部通过后创建 `m1-accepted` 标签。

在以上阻断关闭前，不执行 M2 资产与对象存储开发。

## 7. 本次版本管理

- 验收审计提交：`docs: record M1 total acceptance audit`
- 标签：`m1-total-acceptance-audited`
- 明确不创建：`m1-accepted`
