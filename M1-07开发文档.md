# M1-07 项目完整生命周期开发文档

> Owner：平台工程
> 日期：2026-08-27
> 状态：IMPLEMENTED_LOCAL / PASSED_LOCAL / BLOCKED_EXTERNAL

## 1. 目标与边界

M1-07 实现项目 CRUD、归档/恢复、项目成员、游标分页和 `version/If-Match` 乐观锁。审计、幂等、Outbox 属于 M1-08，RLS 属于 M1-09，本阶段不提前混入。

## 2. 数据模型

- `projects` 新增 `description`、`version`、`archived_at`、`deleted_at`。
- `version` 从 1 开始，由 SQLAlchemy version column 在更新时原子递增，UPDATE 自动携带旧版本条件。
- 新增 `project_members`，角色为 ADMIN/ENGINEER/VIEWER，项目与用户组合主键。
- 新增企业、状态、更新时间复合索引和项目成员企业索引。
- Alembic head：`20260827_05`。

## 3. API 与权限

- 企业 Owner/Admin 可创建项目，创建者自动成为项目 ADMIN。
- 企业 Owner/Admin 可访问和管理企业内全部项目。
- 普通企业成员仅可访问自身已加入的项目；未授权或跨企业统一隐藏为 404。
- 项目 ADMIN 可管理成员；只能添加当前企业成员。
- 列表保持数组响应兼容，通过 `X-Next-Cursor` 返回 opaque cursor；`limit` 为 1–100，默认 20。
- GET/写响应使用 ETag；PATCH、归档、恢复和软删除要求 `If-Match: "<version>"`。
- 缺少 If-Match 返回 428；旧版本或并发失败返回 412 `VERSION_MISMATCH`。
- 删除采用 `deleted_at` 软删除，后续读取和列表不再可见。

## 4. 并发控制

项目模型配置 SQLAlchemy `version_id_col`。两个事务使用相同版本并发更新时，数据库 UPDATE 的版本谓词只允许一个成功；另一事务检测到零更新行后回滚并转换为 412。测试不依赖进程内锁，因此对多进程/多实例同样成立。

## 5. 验收结果

- M1-07 PostgreSQL 专项：3 passed。
- PostgreSQL 全量：50 passed。
- 本地 CI：API 166 passed、21 deselected；Web 5 passed；Web build、Go、文档及部署契约通过。
- OpenAPI 身份输入策略和快照一致性通过。
- Demo 迁移、API 重建和健康证据见验收记录。
- 外部远端 CI、PR 和 Staging 签章未获授权，不标记 ACCEPTED。

## 6. 版本管理

- 基线：`2c3163d` / `m1-06-last-owner-protected`
- 提交信息：`feat: implement M1-07 project lifecycle`
- 标签：`m1-07-project-lifecycle-implemented`

## 7. 后续计划

下一阶段强制执行 M1-08 审计、幂等与 Outbox；随后执行 M1-09 租户纵深防御。M1-08、M1-09 均通过前不得进入资产/任务域。
