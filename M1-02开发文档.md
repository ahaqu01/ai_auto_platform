# M1-02 开发文档

> Owner：平台工程
> 日期：2026-08-27
> 状态：IMPLEMENTED_LOCAL
> 前置版本：`dd95fb4`（`m1-pre-01-hardening-implemented`）

## 目标

按照 ADR-0002 为浏览器管理台提供 OIDC Authorization Code + PKCE 的 BFF 登录闭环。浏览器只持有不透明 HttpOnly Cookie；Access Token、Refresh Token 和 ID Token 仅保存在服务端 Redis。非浏览器客户端继续使用 Bearer Token。

## 对外端点

- `GET /auth/login?return_to=/...`：创建 300 秒一次性登录事务，生成 state、nonce、PKCE verifier/challenge，并重定向 Keycloak。
- `GET /auth/callback?code=...&state=...`：原子消费 state、交换令牌、校验 Access Token 与 nonce，创建服务端会话并设置 Cookie。
- `GET /auth/session`：返回登录状态、显示用用户资料与会话绑定 CSRF Token，不返回任何 OIDC Token。
- `POST /auth/logout`：校验 Origin 与 CSRF，尽力吊销 Keycloak Refresh Token，始终删除本地会话和 Cookie。

## 会话与安全边界

1. Redis key 分为 `bff:login:*` 和 `bff:session:*`；登录事务使用 `GETDEL` 保证 state 只能消费一次。
2. Cookie 默认名为 `platform_session`，`HttpOnly`、`SameSite=Lax`、`Path=/`；Staging/Production 强制 `Secure`。
3. return target 只接受单斜杠开头的站内路径，外部 URL 与 `//host` 回退到 `/`。
4. Cookie 认证的 POST/PUT/PATCH/DELETE 必须同时满足精确 Origin 与会话 CSRF Token；Bearer 请求保持机器客户端兼容。
5. Access Token 与 ID Token 分别按 `platform-api`、`platform-bff` audience 验证 issuer/signature，并校验 nonce；Access Token 到期前 60 秒触发刷新，失败删除会话。
6. Keycloak 吊销失败不阻塞本地退出，避免身份服务故障导致浏览器无法清除会话。
7. 浏览器端不读取 Cookie、不保存 Token，只调用 `/auth/session` 并携带 CSRF 请求退出。

## 本地与部署配置

新增配置：

- `KEYCLOAK_INTERNAL_ISSUER`：容器内 token/JWKS 地址；令牌 issuer 仍按 `KEYCLOAK_ISSUER` 校验。
- `BFF_CLIENT_ID`、`BFF_CLIENT_SECRET`。
- `BFF_PUBLIC_ORIGIN`、`BFF_CALLBACK_URL`。
- `BFF_COOKIE_NAME`、`BFF_SESSION_TTL_SECONDS`。

Demo Nginx 对 `/auth/login`、`/auth/callback`、`/auth/session`、`/auth/logout` 使用精确匹配代理 API，其余 `/auth/` 继续代理 Keycloak。`httpx` 和 `redis` 为 API 生产依赖；服务器 `.demo-cache/wheels` 已补充离线构建缓存，但缓存不进入 Git。

## 管理台行为

管理台启动时读取 `/auth/session`：未登录显示“登录”，已登录显示用户简称和“退出”。退出请求带同源 Origin、Cookie 和 CSRF Token。前端状态不包含 Access Token 或 Refresh Token。

## 测试与验收

- API 自动化覆盖 PKCE、外部 return target、一次性 state、HttpOnly Cookie、会话读取、CSRF/Origin、退出、Cookie 业务 API 与 Bearer 回归。
- Web 自动化覆盖会话加载、无 Token 暴露、退出请求和现有管理台渲染。
- 确定性 OpenAPI 快照已纳入四个认证端点。
- Demo 验证覆盖全部容器健康、真实 Keycloak discovery/login form、Nginx 精确路由、Redis state 与 300 秒自然过期。

本阶段未向仓库写入固定演示账户密码。真实 Keycloak 用户凭据登录可由人工使用受控测试账户补充；callback/令牌交换/Cookie 生命周期由自动化隔离桩验收。

## 后续边界

`M1-03` 负责删除邮箱唯一约束，以及将身份同步改为 PostgreSQL 原子 upsert 并完成并发首次登录测试。本包不提前修改身份合并规则。
