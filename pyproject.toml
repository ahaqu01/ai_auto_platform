[tool.ruff]
line-length = 100
target-version = "py312"

[tool.pytest.ini_options]
testpaths = ["apps/api/tests"]
addopts = "-q"
markers = [
  "postgresql: requires an explicit TEST_DATABASE_URL and a live PostgreSQL server",
]
